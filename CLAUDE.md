<!--
  파생본. 정본은 `공용-프로젝트-컨텍스트.md` (현재 v15).
  v15 전문에서 코드 판단을 바꾸는 규칙만 추린 것이다.
-->

## 이 파일의 위치

### U11 우선 규칙 (2026-09-08)

- 최신 검증은 `python -m src.models.u11_evaluate`; CSV 재검사는 `python -m src.report.u11_report`.
- 학습/보정/test를 분리하고 `target_time`으로 purge한다. 같은 모델을 여러 과거 fold에 재사용하지 않는다.
- 운영 상태 마스크는 반드시 bool. 전체 포함 건수·표본 수는 하위 구간 합계와 일치해야 한다.
- 순위는 `src/serve/ranking.py`를 공유한다. A=강등 없음, B=확률>0.5, C=현재 점유율≥90%.
- 변경률 분모는 질의 수다. 회피 성공은 원래 1위가 혼잡하고 대안 1위가 실제 여유일 때다.
- `predict_proba[:,1]`만 만차 확률로 사용한다. log-odds Platt 보정을 적용한다.
- 구간은 보정에 쓴 것과 동일한 모델로 예측한다. 노출 게이트가 없거나 미달이면 p10/p90은 null.
- 최신 수치·상태는 `reports/tables/u11_report.md`와 `u11_status.json`에서 읽는다.
  미달·평가불가가 남으면 partial. 아래 과거 완료 선언·40/40 표현을 최신 성과로 재사용하지 않는다.

- **정본은 `공용-프로젝트-컨텍스트.md` 다. 충돌하면 정본이 이긴다.**
  **단 아래 「⚠️ 정본과 다른 실측 2건」은 예외다** — 측정으로 반증된 항목이다.
- 여기엔 **코드 판단을 바꾸는 것만** 담는다. 배점표·사업계획서 전략·수상작·상세 일정은
  정본에만 있으니 그런 작업이면 **정본을 먼저 읽는다.**

## 프로젝트 — 주차장 추천 서비스
목적지 + 주차 시간을 넣으면 **도착할 때** 자리 있을 공영주차장을 **요금까지 계산해** 추천한다.
기존 앱은 *지금* 빈자리를 보여준다. 우리는 *도착 시점*을 예측하고 *주차 시간별 요금*을 계산한다.
경기 15개 시군 500곳으로 학습해 안양 89곳의 정확도를 올린다.
**기능 동결 9/17 · 서류 9/21 · 발표 10/30.** "구현 예정" 표현 금지.

## ⚠️ 정본과 다른 실측 (2026-09-04~06, 이쪽이 맞다)

| 정본 v11 | 실측 결과 |
|---|---|
| §5-④ · §13 「GITS `avblPklotCnt` 는 **잔여**다. `(총−잔여)/총`」 | ❌ **틀렸다. `avbl` 은 주차 대수다.** `occ = avbl / pklotCnt` — 도시공사와 **같은** 규약 |
| §5-④-2 「GITS 안양이 85곳, 도시공사와 4곳 차이. 원인 미확인」 | 구조적 차이가 아니다. **스냅샷마다 81~88곳으로 흔들린다**(중앙 83, 68스냅샷). 기본정보엔 89곳 다 있다 |

**부호 근거:** 안양 89곳이 두 출처에 다 있어 같은 시각을 직접 대조했다.
안양6동5노외 96=96 · 박달고가밑 34=34 · 호원어린이공원 20=20.
`|GITS−도시공사|` 중앙 **0.000** vs `|(1−GITS)−도시공사|` **0.632**.
⚠️ **범위 검사(0~1)로는 부호 뒤집힘을 못 잡는다** — 올바른 식·뒤집은 식 둘 다 이상치 1.3%다.
두 출처 교차 대조만이 유효한 검출기다 → `tests/test_occupancy_sign.py`
⚠️ 안양 외 14개 시군은 규약 미검증. 야간 데이터로 판별할 것.

**85곳의 함의:** 실시간이 매번 전 곳을 주지 않는다.
**`occ_lag_*` 계산 시 직전 스냅샷에 그 주차장이 없을 수 있다** — 결측 처리 필수.

**(v11 에서 해소됨)** 「위탁은 GITS `pkplcTypeNm` 으로 덮어쓸 것」은 정본 §13 에
「GITS 로 위탁 유형 판정 금지」로 반영됐다. 주차장명 규칙을 쓴다.

## 데이터

| 출처 | 곳 | 라벨 | 역할 |
|---|---|---|---|
| **GITS 실시간** | 813 (노외 500) | ⭕ 5분 | **학습 표본 + 라벨** |
| 도시공사 포털 | 89 | ⭕ 5분 | 안양 라벨 (GITS와 동일 원천) |
| GITS 기본정보 | 1,010 | ❌ | 유형·요금·운영시간 |
| 표준데이터 | 107 | ❌ | 안양 공영 모집단·급지 |
| 공단 시설정보 | 7,456 / 293,466면 | ❌ | 반경 500m 경쟁 피처 |
| 공단 실시간 | **0** | ❌ | **폐기** |

- **GITS 곳 수는 `(lae_id, pkplc_id)` 조합으로 센다.** `pkplc_id` 만 쓰면 15개 시군이 뭉개져
  152곳으로 나온다(정답 823). PK 는 `(ts_kst, lae_id, pkplc_id)`.
- `prk_center_id` 는 유니크 아님(안양 내 중복 32건). PK 로 쓰지 말 것.
- **점유율 이상치가 실재한다** — 잔여<0 8건(성남 7) · 총면수≤0 8건 · 잔여>총 3건.
  학습 전 반드시 제거. 시군별 품질 차이는 **도시 고정효과의 근거**이기도 하다.

### 공단 API — 실측 확정 (재검증 금지)
`numOfRows` 상한 **50,000** · 지역 필터 **없음** · items 는 `j["오퍼레이션명"]` ·
`totalCount` 과대보고(1,767,934 신고 / 실수신 1,508,686) · 268B XML 은 게이트웨이 타임아웃 →
재시도로 흡수 · 키에 `%` 있으면 `unquote` · `is_anyang` 은 `sigungu.startswith("안양")`

### GITS
```
https://openapigits.gg.go.kr/api/rest/{getParkingPlaceAvailabilityInfoList|getParkingPlaceInfoList}
serviceKey(필수) · laeId(선택)   ※ apiKey/key/authKey 는 인식 안 됨
응답 XML: ServiceResult > msgBody > itemList*   (JSON 없음)
headerCd  0=정상 · 5=키 없음 · 7=키 사용중지 · 8=요청제한 초과
```

### 카카오
- 좌표는 **`경도,위도`** 순. 다중목적지는 `POST /v1/destinations/directions`, 최대 30곳,
  **`key` 로 매칭**(순서 의존 금지). 미래운행은 `departure_time=YYYYMMDDHHMM`(미래 필수).
- **쿼터 응답 헤더가 없다.** 잔여 호출수는 우리가 센다.
- 로컬 PK6 는 **질의당 45건 상한**(15×3페이지). `total_count` 가 더 커도 45건만 온다.

## ★ 요금 계산 — 조례 별표 기반 누진제
`src/serve/fare_tables.py` · `src/serve/fare.py` · 테스트 `tests/test_fare.py` (33건 통과)

**「기본요금 + 고정 단가」는 틀렸다.** 2시간 −35%, 3시간 −51% 과소계산된다.
GITS `addUnitFare` 는 **첫 구간(30~60분) 값 하나**다. 전 구간에 곱하지 말 것.

```
운영시간 내/외 분리 → 15분 미만 면제 → 누진(10분 단위 올림)
→ 일 상한 25,000 → 감면 → 100원 절사(감면 후)
```

**원본 HWP 파싱으로만 나온 조항 3개 — 전부 반영됨**
- 비고 10: **누진 일 최대 상한 25,000원** ← `oneday_amt` 25,000 의 정체(월정기권 아님)
- 비고 11: **100원 미만 절사** — 감면 **후**에 적용
- 비고 4: **일요일·법정공휴일 무료** (인자로 끌 수 있게 함)

### ★ 일일권은 자동 상한이 아니라 **선불 상품** (비고 8 · v11 확정)

`min(계산액, 일일권)` 은 틀렸다. **두 금액을 병기하고 「입차 시 구매」를 안내한다.**

| 노외 1급지 13시간 | 후불(누진) | 선불 일일권 | 권장 |
|---|---:|---:|---|
| 120분 | 5,100 | 16,000 | 후불 |
| **221분** | 16,500 | 16,000 | **선불로 전환** |
| 300분 | 24,900 | 16,000 | 선불 (**8,900원 절약**) |

`calc_fare` 반환: `total`(후불) · `total_prepaid`(선불) · `recommend_prepaid` · `prepaid_saving`
· `daily_pass_better_after_min`(=221분). `apply_daily_pass_cap=True` 로 v10 동작 재현 가능.
별표2-2 에 따라 **1일주차 요금은 감면 70%** 라 선불액은 따로 계산한다.

**`oneday_amt` 는 참고용, 별표 5 재계산값이 정본.** 표 안 80곳 중 74곳 일치(92.5%),
6곳 불일치(2곳은 일일권 자리에 일 상한 25,000 이 들어가 있다).
운영 8h 미만·13h 초과는 **보간 금지** → `None`, 상한 없음 + 로그. 24시간 운영 9곳이 해당.

**유형은 주차장명 규칙으로 해소한다** — GITS `pkplcTypeNm` 은 위탁 14곳을 전부 '기타'로 준다.
`resolve_type()`: 표준데이터 > 이름(노상 포함→노상 / 지하→노외) > `div`. 결과 노상 13 / 노외 1.

## ★ 도착시간 정책 (결정 ⑨)
**카카오 실제값만 쓴다. 폴백 상수 재보정 금지.**
출발지 1곳·25경로로 맞춘 값은 근거가 얇고, 제대로 하려면 별도 연구다.

| | 정책 |
|---|---|
| 정상 | 카카오 `multi_eta` / `future_eta` 실제값 |
| 장애 시 | 거친 근사로 서비스 유지 + **`estimated: True`** 반환 |
| UI | **"실시간 경로 조회 실패 — 추정치"** 명시 |
| 폴백 항목 | **만차확률 기반 순위 강등을 적용하지 않는다** |
| 후보 필터링 | 직선거리 사용 — 이건 폴백이 아니라 **정상 로직** |

## 모델링 규칙
- **lag 해금.** 타깃이 라벨 있는 89곳이라 자기회귀가 최강 피처다. v8 의 lag 금지는 무효.
- `horizon_min`(15/30/60/120)을 **피처로 넣은 단일 모델**. 4개 따로 만들지 않는다.
- `city` 고정효과를 넣는다. 최종 모델은 500곳 전부로 학습하고,
  **모델 선택 기준은 「안양 89곳 MAE 최소」**(외부가 해가 되면 자동으로 안 씀).
- **leave-one-city-out 은 검증 방법이지 학습 방법이 아니다.**
- 검증: **시간 순차 분할** + LOCO. 랜덤 분할 절대 금지.
- 분위 예측은 Δ의 p10/p50/p90에 쓴다. `full_prob`는 분위 보간이나 계단형 isotonic이 아니라
  **`occ(t+h) >= 90` 직접 LightGBM binary 분류기**의 확률을 쓴다. 품질은 AUC가 아니라
  Brier score와 10-bin reliability diagram으로 persistence와 함께 점검한다.
- **독립 표본은 lot 수.** 피처 15~20개, LightGBM/XGBoost 상한. 딥러닝 금지.
- 모든 상관계수에 **n 과 95% 신뢰구간(Fisher z)** 병기. `spearman_ci()` 사용.
- **학습 집단을 평가 집단과 같이 좁히지 말 것** — 타깃 조건을 재현할 대상은 평가지 학습이 아니다.
- **기준선을 바꿔가며 개선폭 비교 금지** (v8 에서 −1.0%p 착시 발생).

### ★ v15 에서 확정된 것 (측정 결과)
- **타깃은 레벨이 아니라 Δ 다.** `y = occ(t+h) − occ(t)`, 복원은 `clip(occ(t)+Δ̂, 0, 120)`.
  **복원 시 클리핑을 빠뜨리지 말 것.**
  ⚠️ 「Δ 는 persistence 보다 구조적으로 나빠질 수 없다」는 **틀렸다.**
  L2 목적함수로는 2회 졌다. 정확히는 **「목적함수를 MAE 에 맞추면(=L1) 나빠지지 않는다」.**
- **평가는 반드시 층화한다.** 살아있는 67곳 × 평일/주말 × 운영중/운영외.
  전체 평균 하나만 보면 안 된다 — A15 의 −42% 는 **평일 장기 horizon 에서만** 성립했고
  주말에선 +107~223% 로 무너졌다.
- **분할이 구조적으로 제약된다.** 6일 중 마지막 2일이 토·일이라
  train 주말과 test 주말을 동시에 확보하는 시간순 분할이 없다
  (A_80: train 주말 5.9% / test 토·일만, B_70: train 주말 0%).
  **A_80 에서 「평일 지표」를 뽑으려 하지 말 것 — test 에 평일이 없다.**
- **운영시간 피처는 `target_time`(도착 시각) 기준으로 만든다.** 관측 시각이 아니다.
  `src/features/temporal.py` · 주말 MAE −33%, 평일은 +0.8~8.8% 손해지만 절대값이 작아 항상 켠다.
- **만차 AUC 는 persistence 도 0.99 다.** 모델 AUC 0.99 를 성과로 보고하지 말 것.
  반드시 persistence 기준선을 병기한다. `full_prob` 의 가치는 판별력이 아니라 **보정된 확률값**이다.
- **`DEAD_FEED_IDS` — 실시간이 죽은 22곳은 순위에서 제외한다.**
  점유율 범위가 정확히 0.00 이다. 위탁 14곳이 전부 여기 들어간다.
  **GITS 로 되살릴 수 없다**(같은 소스라 똑같이 0.00). 학습·평가·추천 모두에서 뺀다.
- 대표 성능 숫자는 **운영 중 × 살아있는 67곳**이며, 운영 외도 같은 표에서 항상 병기한다.
  운영 외를 사용자 부재로 해석하지 않는다. 운영외 변화량이 작아 persistence가 유리할 수 있다는
  가설은 validation의 (horizon, 운영 중/외) 8칸 MAE로만 선택한다(`data/processed/router.json`).
  조건문으로 운영외 persistence를 고정하지 않는다.
- 죽은 피드는 `by_fare`/`by_walk`에서는 제외하지만 `unavailable[]`에는 같은 카드 스키마로 반환한다.
  점유율·예측·만차확률만 null이고, 요금·차/도보·면수·운영시간은 보여 준다.

### 피처
- **A 자기회귀** `occ_lag_5m/30m/1h/1d/1w` · `occ_trend_30m` · `occ_ma_1h`
- **B 시간** `horizon_min` · `hour_sin/cos` · `dow` · `is_weekend` · `is_holiday` ·
  `is_operating` · `hours_since_open` · `mins_to_close`
- **C 정적** `log_cells` · `is_nosang` · `is_public` · `is_underground` · `open_hours` · `city`
- **D 공간** `compet_n_500` · `compet_cells_500` · `nearest_station_dist`
- **E 환경** `temp` · `rain`
- **제외 확정:** `grade` · `oneday_amt` · `hourly_rate` · `hndcap_ratio` · `is_wital` ·
  **용도지역 · 공시지가**(측정 결과 개선 0.0%p)

## 성능 목표
30분 MAE ≤ **8%p** · 60분 ≤ 12%p · 120분 ≤ 15%p ·
만차 판정 AUC ≥ 0.85 & 정밀도 ≥ 0.8 · 캘리브레이션 기울기 0.9~1.1
⚠️ **AUC 0.85 목표는 이 문제에 너무 낮다** — persistence 만으로 0.99 가 나온다.
달성 여부가 아니라 **persistence 대비 차이**로 판단할 것.

## 인프라
- 안양 89곳: GCP `anyang-poller`(us-east1-c / `anyang-parking-507119` / `chabee2027`),
  systemd `anyang-poll`, 5분, 2026-08-31 04:56~
- GITS 813곳: systemd `anyang-gits`, 5분 + `headerCd 8` 시 600초 자동 감속(연속 3회 성공 시 복귀)
- **폴링은 벽시계 격자 정렬.** `sleep(INTERVAL)` 을 쓰면 작업 시간이 누적돼 하루 15분씩 밀린다.
- 회수: `bash scripts/pull_vm.sh` — **돌아가는 DB 를 `.backup` 없이 복사하면 깨진다.**
  cron 이 3시간마다 `scripts/auto_pull.sh` 를 돌린다(노트북 켜져 있을 때만).
- systemd 유닛은 **따옴표 없는 히어독**으로 쓸 것. `<<'EOF'` 로 쓰면 `$USER`/`$HOME` 이
  치환되지 않아 `status=209/STDOUT` 무한 재시작이 난다.
- 파이썬 히어독은 반대로 `cat > 파일 <<'PYEOF'` (따옴표 있음).

## 코드 규칙
- 시드 고정(`RANDOM_STATE=42`). 경로는 config 에서, 하드코딩 금지.
- 산출물은 `reports/figures/*.png` · `reports/tables/*.csv`. **파일명에 날짜 금지.**
- 한글 폰트: `AppleGothic`(맥) / `NanumGothic`.
- **컬럼명·응답 키를 추측하지 말 것.** 출력해 확인하고 그 키로만 접근한다.
  이 프로젝트에서 필드명 가정으로 세 번 틀렸다.
- **pandas 이름 충돌 주의** — `df.div` · `df.at` · `s.std` 는 메서드다.
  컬럼 접근은 `df["div"]` 로. `itertuples` 는 밑줄로 시작하는 컬럼명을 `_1` 로 바꾼다.
- **`.gitignore` 에 같은 줄 주석 금지** — 패턴에 포함돼 규칙이 깨진다.
- `data/` 는 통째로 gitignore. 팀원과는 별도 경로로 공유한다.
- 큰 배치는 스트리밍(JSONL) + 체크포인트 + 페이지 단위 예외 처리.

## 하지 말 것
**요금** — `addUnitFare` 전 구간 곱하기 · `ddPktckFare` 사용 · 별표5 표 밖 보간 ·
조례표를 API 에서 가져오려 시도(어느 API에도 누진 단계가 없다) · 부설 요금을 추천에 사용 ·
**일일권을 `min()` 으로 자동 적용**(선불 상품이다) · 일요일·공휴일에 요금 부과 ·
감면 **전에** 100원 절사 · **GITS `pkplcTypeNm` 으로 위탁 유형 판정**

**도착시간** — 폴백 상수 재보정 · 폴백인데 추정치임을 숨기기 · 카카오 응답을 순서로 매칭

**부호·단위** — GITS 곳 수를 `pkplc_id` 만으로 세기 · 점유율 이상치를 안 걸르고 학습 ·
카카오 좌표를 위도,경도 순으로 · 면적을 WGS84 로 계산

**피처** — 타깃에 없는 피처 포함 · 채움률 확인 없이 표준데이터 투입 ·
`grade`·용도지역·공시지가 재시도

**검증** — 랜덤 분할 · 신뢰구간 없는 상관 보고 · 관측치 많다고 딥러닝

**데이터** — "변동 0"을 계측 불량으로 단정(항상 만차일 수 있다) · 5분보다 짧은 폴링 ·
`.backup` 없이 DB 복사 · 공단 `numOfRows`·지역 필터 재탐색 · 곳 수를 행 수로 세기

**서비스** — 부설을 대체 주차장에 표시 · 카카오 실패 시 서비스가 같이 죽는 구조 ·
최저요금·최단거리를 가중합으로 섞기 · `reports/` 파일명에 날짜
