<!--
  파생본. 정본은 리포 루트 `공용-프로젝트-컨텍스트.md` (현재 v7).
  v7 부터 정본에 "CLAUDE.md 복붙" 블록이 사라져, 이 파일은 v7 전문에서
  코딩 판단을 바꾸는 규칙만 추려 재구성한 것이다.
-->

## 이 파일의 위치
- **정본은 `공용-프로젝트-컨텍스트.md` 다. 충돌하면 무조건 정본이 이긴다.**
- 여기엔 **코드 판단을 바꾸는 것만** 담는다. 배점표·사업계획서 전략·수상작 분석·상세 일정은
  정본에만 있으니, 그런 작업이면 **정본을 먼저 읽는다.**
- 정본이 갱신되면 이 파일도 다시 추려 쓰고, 바뀐 내용이 기존 코드에 미치는 영향을 보고한다.

## 프로젝트
2026 안양시 공공데이터·AI 경진대회 출품작.
유료 주차장은 앱이 자리를 알려주지만 **무료 주차장은 아무도 안 알려준다.**
계측되는 89곳으로 학습해 계측 안 되는 구간(무료 노외 ~18~25곳)의 점유율을 추정한다.
**기능 동결 9/17 · 서류 제출 9/21 · 발표평가 10/30.** "구현 예정" 표현 금지.

## ✅ 확정된 결정 (9/1, 재론 금지)
- **공영 한정.** 민영·부설은 반경 500m 경쟁 피처로만 쓴다. 급지·유형이 있는 건 표준데이터 107곳뿐이고
  공단 7,456곳 기준 결측률 98.5% — MNAR 이라 평균 대체하면 `grade` 가 "공영 여부"를 코딩한다.
  부설은 생성 과정도 반대다(아파트는 밤에 차고 낮에 빈다). 한 모델에 넣으면 shape 학습이 망가진다.
- **안양 한정.** 경기도로 넓힐 데이터가 존재하지 않는다(공단 실시간 78만 중 경기 155개, 안양 0곳).
- **B2G 행정 지표가 메인 산출물.** level 순위 0.665로 합격선을 이미 넘겼다. 시민 신호등은 보조.
- **모델 A(자기회귀·시민 실시간)는 2인 팀이라 잘라냈다.** 모델 B(환경 피처만)만 만든다.

## 데이터 — 세 출처, 각각 여기서만 얻는 것

| | 도시공사 포털 89곳 | 표준데이터 107곳 | 공단 시설 7,456곳 |
|---|---|---|---|
| 실시간 점유율 | ⭕ **유일한 라벨** | ❌ | ❌ (연계 0곳) |
| 급지 | ❌ | ⭕ **여기만** | ❌ |
| 노상/노외 | △ "위탁" 섞임 | ⭕ **정본** | ❌ |
| 좌표 | ⭕ 결측 0 | △ 47/107 | ⭕ |
| 주변 경쟁 면수 | ❌ | ❌ | ⭕ **여기만** |

**★ 전이 타깃은 표준데이터에만 있다 → 피처 출처가 표준데이터로 고정된다.**

**병합 우선순위 (`build.py` 에 명시)**
```
유형·급지·공영여부  →  표준데이터가 이김
요금·면수 세부      →  도시공사가 이김 (단 타깃엔 없음 → 피처로 못 씀)
주변 경쟁 집계      →  공단이 이김
좌표                →  도시공사 → 공단 → 지오코딩
```

- 라벨: `https://parking.auc.or.kr/api/parking/searchParkingList` → `data/raw/parking.db`
  `PARK_COUNT / CELL_CNT`. **정식 개방 데이터가 아닌 내부 API — 신청서 기재 금지.** 폴링 5분 이상.
- 표준데이터 31개 컬럼 중 실사용 ~12개. **9개는 전부 공란, 6개는 값 한 종류.**
  **쓰기 전에 반드시 채움률부터 센다.**
- 곳 수는 **항상 고유 `prk_center_id` 기준**. 유니크하지 않다(안양 내 중복 32건).

### 공단 API — 실측 확정 사양 (재검증 금지)
`https://apis.data.go.kr/B553881/Parking` · 일일 10,000회 × 엔드포인트 3개 각각
- 지역·ID 필터 파라미터 **없음**. pageNo 방식이 유일
- `numOfRows` 상한 **50,000** (문서의 1,000은 무의미)
- 268B XML / 60초 응답은 **게이트웨이 타임아웃 → 재시도로 흡수**
- items 위치는 `j["오퍼레이션명"]`. `body["items"]` 아님
- `totalCount` 과대 보고 (1,767,934 신고 / 실수신 1,508,686 = 85%)
- 인증키에 `%` 있으면 `unquote` — 인코딩 키를 params 로 넘기면 이중 인코딩된다
- `is_anyang` 은 **`prk_plce_adres_sigungu.startswith("안양")`** (주소 문자열 매칭은 "안양판교로" 오탐)

## 인프라
- 폴러: GCP e2-micro `anyang-poller` / `us-east1-c` / `anyang-parking-507119` / `chabee2027`
  systemd `anyang-poll`, `Restart=always`, 5분, 2026-08-31 04:56 KST~
- 회수: `bash scripts/pull_vm.sh` — **돌아가는 DB를 `.backup` 없이 복사하면 깨진다**
- `obs PRIMARY KEY(ts_kst, parking_id)` 라 여러 곳에서 폴링해도 병합 시 중복 자동 제거
- 히어독은 `cat > 파일 <<'PYEOF'` (따옴표 있음) — 없으면 변수 치환된다

## ⚠️ 포털 `div` 의 "위탁" 함정
`PARKING_DIV_NM` 에 노상/노외와 함께 **"위탁"**이 섞여 있는데 이건 위치가 아니라 **운영 방식**이다.
표준데이터 대조 결과 위탁 14곳 중 13곳이 실제로는 노상. **놓치면 조용히 성능이 깎인다.**
```python
df["type"]     = 노상 / 노외    # 위치 — build.py 에서 표준데이터로 덮어쓸 것
df["operator"] = 직영 / 위탁    # 운영 방식
```

## 모델링 규칙 (위반 금지)
- **점유율 = level(lot) × shape(lot,t).** 구조는 검증됐다(오라클 MAE 4.3%p).
  틀린 게 아니라 **shape 을 예측할 재료가 없는 것**이다.
- **모델 B 에 자기회귀 lag 금지.** 넣는 순간 라벨 없는 곳에 못 쓴다.
- **피처는 "학습 데이터에 있나"가 아니라 "타깃에 있나"로 고른다.**
- 유료 주차장의 운영시간 외(무료) 구간을 반드시 학습에 포함 → 무료 타깃이 외삽 아닌 내삽이 된다.
  **관측의 50.4% 가 운영시간 외라 이미 확보됨.**
- 검증: **Leave-One-Location-Out CV.** 랜덤 8:2 분할 절대 금지(인접 시각 누수).
- **분위 예측**(LightGBM `objective='quantile'` 10/50/90) + **isotonic calibration** 필수.
- **독립 표본은 lot 수(67).** 피처 10~15개, LightGBM/XGBoost 상한. 딥러닝 금지.
- 피처 선택은 between-lot / within-lot 분리 평가. 단순 Pearson 금지.
  쌍(pair) 상관에 일반 p값 금지 — **Mantel 순열** 필수.

## 확정 피처 9개 (9/1)
`is_nosang`(+0.623 최강) · `grade`(−0.407) · `log_cells`(−0.332) · `is_underground`(−0.306) ·
`open_hours` · `is_transfer` · `is_manan` · `compet_n_500`△ · `compet_cells_500`△
(△ = 타깃 좌표 60곳 결측, 지오코딩 후 가능) · **VIF 최대 2.30, 공선성 해소됨**

**탈락 4개 — 전부 "타깃에 값이 없다"**: `hndcap_ratio`(−0.368) · `oneday_amt`(+0.410) ·
`hourly_rate`(+0.410) 는 표준데이터 107곳 전부 공란, `is_wital` 은 표준데이터에 개념 자체가 없음.
**성능이 아까워도 뺀다.**

**shape 을 설명할 후보(17.4%p 오차를 줄일 유일한 레버)**: 반경 500m 세대수 / 상가 수 / 업종 구성비 /
최근접 역 거리 / 요일·주말. 가장 유망한 파생: **주거상업비 = 세대수 ÷ (세대수 + 상가 수)**.
근거: 운영시간 외 점유율이 운영중의 1.6배(0.711 vs 0.457) — 야간 거주자 주차.
⚠️ 상가 데이터는 행정동(31개) 단위라 반경 500m 로 안 쪼개진다. 구분력을 실측으로 확인할 것.

## 현재 성능 (9/1 실측, 34.2시간 · 67곳)
| 방식 | MAE |
|---|---|
| baseline (상수) | 29.9%p |
| level̂ × 글로벌 shape | 20.7%p |
| + `is_operating` | **20.6%p — 시간표는 +0.1%p 밖에 안 준다** |
| oracle | 4.3%p |

오차 분해: **shape 오차 17.4%p > level 오차 13.2%p** → shape 이 병목.
**level Spearman +0.665 → 행정용 합격선 0.6 통과 🟢 / MAE 20.6%p → 15%p 미달 🔴**
ICC 0.307 (0.5 미만이라 "level 지배적"은 미확증). **주말 관측 0 — 9/5~6 이후 게이트 재실행 필수.**

## 코드 규칙
- 시드 고정(`RANDOM_STATE=42`). 경로는 config 에서, 하드코딩 금지.
- 산출물은 `reports/figures/*.png`, `reports/tables/*.csv`. **파일명에 날짜 금지**(사업계획서 링크가 깨진다).
- 한글 폰트: matplotlib `AppleGothic`(맥) / `NanumGothic`.
- 컬럼명·응답 키를 **추측하지 말 것.** 출력해서 확인하고 매핑한다. 필드명 가정으로 두 번 틀렸다.
- 큰 배치는 스트리밍(JSONL)으로 흘리고 메모리에 누적하지 않는다. 체크포인트 + 페이지 단위 예외 처리.

## 하지 말 것
- ❌ 타깃에 없는 피처를 최종 피처셋에 포함
- ❌ 표준데이터 컬럼을 채움률 확인 없이 투입 / MNAR 결측을 평균 대체
- ❌ 모델 B 에 자기회귀 lag
- ❌ 랜덤 train/test 분할 / 단순 Pearson 피처 선택 / 쌍 상관에 일반 p값
- ❌ **상관계수 하나로 프로젝트 판정** — 판정 기준은 LOO MAE, 상관은 진단용
- ❌ 관측치 많다고 딥러닝 (독립 표본은 lot 수)
- ❌ **34시간 데이터로 "계측 불가" 단정** — 멈춘 22곳은 값이 만차인지 0인지부터 확인.
  **만차면 "항상 꽉 참"이라 행정 지표에서 가장 중요한 정보다. 절대 버리지 말 것.**
  확정 판정은 7일치에서 `poll_parking.py --analyze` 로 한다
- ❌ 도시공사 내부 API 를 신청서에 기재
- ❌ 5분보다 짧은 폴링 / `.backup` 없이 DB 복사
- ❌ 포털 `div` 의 "위탁"을 위치 유형으로 사용
- ❌ `prk_center_id` 를 PRIMARY KEY 로 사용
- ❌ 공단 `numOfRows` 상한·지역 필터 재탐색 (50,000 / 없음 으로 확정)
- ❌ 곳 수를 행 수로 세기 — 항상 고유 ID 기준
- ❌ 로드뷰(촬영 시각 랜덤) · 위성(3m 해상도)
- ❌ `reports/` 파일명에 날짜
