# 데이터 (라벨 DB는 비공식 내부 API 수집물 — 절대 커밋 금지)
data/raw/*.db
data/raw/*.db-journal
data/raw/*.db-wal
data/raw/*.db-shm
data/**/*.parquet
# 공단 전수 산출물 — 100MB 초과라 GitHub 거부. API 로 재생성 가능(sttus 31분)
data/raw/kotsa_*_gg.jsonl
data/raw/kotsa_*_idmap.json
data/raw/.ckpt_*.json
# 재생성 가능한 WFS 수집 캐시
data/interim/zoning_cache.csv
data/interim/type_dist_cache.csv

# 파이썬
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/

# 환경·로그
.env
logs/
*.log

# OS
.DS_Store
