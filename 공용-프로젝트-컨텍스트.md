# 2026 안양시 공공데이터·AI 경진대회 — 팀 공용 컨텍스트

**v16 · 2026-09-07 갱신 — ★★ 롤링 오리진 40/40 · 「주말 붕괴」는 분할 아티팩트였다**
🟢 폴링 정상 · 🔴 89곳 중 **22곳 피드 고정** ·
✅ TMAP·요금·후보선정·강등·분위·서비스연결 완료 ·
★★ **5-fold rolling origin 에서 40/40 칸 ML 승** — a17 의 주말 열세는 **검증 설계 결함**이었다

> ## 🔴 지금 당장
>
> **① 라우터 폐기** (§8.6) — 롤링 오리진에서 persistence 가 이기는 칸이 **하나도 없다**.
> 그런데 현재 라우터는 4칸만 남은 채 운영외·주말 15/30/60분을 persistence 로 보내
> **오히려 성능을 깎고 있다.**
>
> **② U9 산출물이 커밋되지 않았다.** 40/40 결과가 untracked CSV 에만 있다.
>
> **③ 죽은 피드 22곳** (§5-①-2) — 6일 내내 변동폭 `0.00`, 행의 24.7%.
> 순위에서 제외하고 「실시간 미제공」 완전 카드로 노출하는 것으로 확정(§3).
>
> **④ 스크린샷 0장** — `web/` UI 가 완성도 20점의 최대 리스크다.

⚠️ **v8까지의 「전이 추정」 설계는 §17로 이관됐다.** 이 문서는 새 설계 기준이다.
변경 이력은 §16 · 발전 방향은 §18.

---

## 0. 한 줄 요약

> **목적지와 주차할 시간을 넣으면, 도착할 때 자리가 있을 공영주차장을 요금까지 계산해서 추천한다.**
>
> 기존 앱은 **지금** 빈자리를 보여준다. 우리는 **도착할 때**를 예측하고, **주차 시간에 따른 요금**까지 계산한다.

경기 15개 시군 500곳으로 학습해서 안양 89곳의 예측 정확도를 올린다.

---

## 1. 마감과 제출물

**오늘 2026-09-06(일) · 마감까지 15일 · 기능 동결까지 11일 · ★★ 수집이 이틀째 멈춰 있다**

| 항목 | 내용 |
|---|---|
| **접수 마감** | **2026. 9. 21.(월)** |
| **시제품** | 마감일까지 완료된 상태여야 함 |
| 사업계획서 | A4 **10p 이내** |
| 제출 방식 | 서식1~5 + 재학증명서를 **PDF 1개(10MB 이하)** 로 이메일 |
| 파일명 | `작품명_팀명.pdf` |
| 재학증명서 | **8/21 이후 발급분만 유효** |
| 서명 | 서식1·3·4·5는 **팀원 전원 자필 서명 후 스캔** |
| 접수 | karyeong@korea.kr |

### "시제품 완료"의 실제 의미 — 2단계

9/21에 내는 건 **PDF 하나**다. 코드·데모 URL 칸이 없고 1·2차는 서류만 본다.
실물 시연은 **3차 발표평가(10/30)** 가 처음.

| 시점 | 목표 |
|---|---|
| **9/17** (진짜 마감) | 기능 동결. **결과 표 + 스크린샷** 확보 |
| 9/21 | 서류 제출 |
| **10/30** | 진짜 완성도. 10분 시연 + 질의응답 |

**"구현 예정"을 쓰면 완성도 20점이 0에 수렴한다.** 있는 만큼만, 대신 확실히.

**보너스:** 발표 시점엔 데이터가 두 달치. *"제출 시 MAE X → 발표 시 Y"* — 수집형만 쓸 수 있는 카드.

일정: 1차 서류(9/22~23, **12팀 컷**) → 2차 서류(9/28~10/13, 4팀) → 공개검증(10/16~27) → 발표(10/30)

---

## 2. 배점표 — 모든 판단의 기준

| 지표 | 배점 | 심사 문구 |
|---|---|---|
| 공공데이터 활용 | **25** | 공공데이터가 **실제로 필요하며** 유의미하게 쓰였나 |
| AI 혁신성 | **20** | **AI 학습·분석 도구**를 효과적으로 활용했나 |
| 완성도 | **20** | 오류 없이 작동하나, UI/UX가 직관적인가 |
| 발전 가능성 | **20** | 실제 **행정** 및 시민 서비스로 도입될 가능성 |
| 독창성 | 15 | 기존 유사 서비스와의 차별점 |

- 안양시 데이터 융합 시 25점 / 일반만 22점. **신청서에 제공기관·데이터명·URL 표 미기재 시 가점 없음.**
- 동점 시: **발전가능성 → 공공데이터 → AI혁신성**
- 3차 발표평가: 완성도 20→15, 발표력 5 신설

### ⚠️ 미해결 ①·② — 안양시 데이터가 한 건도 없다

GITS 확보로 「도시공사 내부 API 기재 금지」는 풀렸는데, **대신 새 문제가 생겼다.**

| 데이터 | 제공기관 |
|---|---|
| GITS 실시간 | **경기도** |
| 경기데이터드림 표준데이터 | **경기도** |
| 공단 시설정보 | 국토교통부 |
| 카카오 길찾기·로컬 | 민간 |

**신청서에 쓸 「안양시」 데이터가 없다.** 25점 가점 조건이 "안양시 데이터 융합"인데,
도시공사(안양시 산하) 대신 GITS(경기도)를 쓰면 오히려 안양시 색이 빠진다.

**대응 (B 담당, 이번 주 안)**
1. 경기데이터드림 안양시 632건 중 **제공기관이 "안양시"인 것을 최소 1건** 찾아 끼운다. 주제 무관
2. 도시공사 API를 **안양도시공사 제공**으로 병기 가능한지 확인

**문의: 안양시 AI정책과 이가령 주무관 031-8045-5483 — 질문 3개**
> ① 표준데이터 제공기관이 "경기도"인데 안양시 칸에 적어도 되나요?
> ② 안양도시공사 주차 API를 안양시 데이터로 적어도 되나요?
> ③ 안양시가 직접 개방한 데이터 목록은 어디서 보나요?

---

## 3. 아이템 — 주차장 추천 서비스

### 사용자 흐름

```
출발지(현위치 또는 지정) + 목적지 + 주차 시간 + [출발 시각]
   ↓
목적지 반경 1km 공영주차장 후보
   └ 5곳 미만이면 2km → 그래도 미만이면 3km 까지만 확장 (그 이상 금지)
   ↓
[지금 출발]  카카오 다중목적지 1회  →  주차장별 차 ETA
[미래 출발]  카카오 미래운행 1회    →  목적지 ETA 하나를 공통 적용
   ↓
모델이 (출발시각 + 차 ETA) 시점 점유율·만차확률 예측     ★ 차 도착 시점
   ↓
TMAP 보행자 경로 → 주차장에서 목적지까지 실제 도보 시간
   ↓
① 최저요금순      ② 도보 최단순         ← 「거리」가 아니라 도보 시간
   ↓
만차확률 > 컷오프 → 강등, 다음 순위가 1순위로
   ↓
카드: "차 12분 + 도보 7분 = 19분 · 2시간 5,100원 · 예상 잔여 8면"
   ↓
「대체 주차장」 — 카카오 PK6, 위치만 표시 (순위 밖)
```

### ★ 시간이 두 종류다 — 섞으면 안 된다

```
차 ETA    →  주차장 도착      ← ★ 혼잡도 예측은 이 시점
도보 시간  →  목적지 도착
──────────────────────────
총 소요                        ← 사용자에게 보여줄 "실제 걸리는 시간"
```
**자리를 찾는 건 주차장에 들어갈 때다.** 목적지 도착 시각으로 예측하면 도보 시간만큼 늦게 본다.

### ★ 반경 확장 — 고정하면 외곽에서 깨진다

89곳 좌표 실측:

| 목적지 | 500m | **1km** | 2km |
|---|---|---|---|
| 안양역 | 5 | **13** | 32 |
| 인덕원역 | 6 | **11** | 25 |
| 안양시청 | 4 | **8** | 35 |
| 평촌역 | 1 | **7** | 34 |
| 범계역 | 2 | **7** | 36 |
| **석수역** | 1 | **1** | **5** |

석수역은 1km에 1곳뿐이다. **1km → (5곳 미만이면) 2km → 3km. 3km가 상한이다.**

### 서비스 철학

**"저렴한 공영주차장을 쓰자."**
추천 순위는 **공영만**. 민영·개방부설은 순위에 넣지 않고 *"여기도 있어요"* 로만 띄운다.

### 배점 재평가

| 항목 | 배점 | v8 (전이 추정) | **v9 (추천 서비스)** |
|---|---|---|---|
| 공공데이터 | 25 | 22 | **20~24** ← 안양시 데이터 확보 여부 |
| AI 혁신성 | 20 | 15 | **17** |
| 완성도 | 20 | 12 (목표 미달) | **18** |
| 발전가능성 | 20 | 16 | **17** |
| 독창성 | 15 | 11 | **12** |
| **합계** | 100 | **76** | **84~88** |

### 폐기 후보 (재론 금지)

주정차 단속 효율화(안양 2024 최우수) / 지역화폐·상권(안양 2024 우수 2건) /
교통약자 이동(경기권 2025에 5건) / 폭염 쉼터(안산 2026 최우수) / 보행약자 지도(안산 2025 최우수)

**997건 재대조 결과 — 주차 관련 13건은 전부 「단속」 또는 「수요 분석·입지」다.
시민 대면 추천 서비스는 0건.** 근거: `claude/기존수상작-충돌검증.md`

---

## 4. ✅ 확정된 결정 (9/4)

| # | 결정 | 근거 |
|---|---|---|
| ① | **아이템 = 추천 서비스** | 배점 76 → 84~88 |
| ② | **폴링 5분 · GCP · `headerCd 8` 백오프** | 한도 미확인이라 자동 감속 |
| ③ | **학습 = 경기 15개 시군 500곳** | v8의 "안양 한정"은 무효 (아래) |
| ④ | **최종 모델은 「안양 89곳 MAE 최소」로 선택** | 외부가 해가 되면 자동으로 안 씀 |
| ⑤ | **horizon을 피처로 넣은 단일 모델** | 15/30/60/120분. 4개 따로는 2인 팀에 무리 |
| ⑥ | **대체 주차장 = 카카오 로컬 PK6** | 부설 자동 제외. 유형 판정 실패를 우회 |
| ⑦ | **랭킹 = 최저요금 / 최단거리 2축 + 만차확률 필터** | 가중합으로 섞으면 이유를 못 읽음 |
| ⑧ | **컷오프는 점유율이 아니라 만차확률** | 값은 캘리브레이션 보고 결정 |
| ⑨ | **도착시간 = 카카오 실제값만 사용** | 폴백 상수 튜닝은 하지 않는다 (아래) |
| ⑩ | **현재값 표시 + 도착시각 예측 재정렬** | 카카오맵 + 네이버지도 조합 |

### ★ 결정 ③ 재개 — v8의 "안양 한정"이 무효가 된 이유

v8은 *"넓힐 데이터가 존재하지 않는다"* 였는데, **그건 공단 API만 보고 내린 판단**이었다.
지자체 자체 개방 경로를 확인한 적이 없었고, 확인해 보니 **있다** (§5-④ GITS).

```
GITS 실시간 노외 500곳 = 안양 49 + 외부 451 (14개 시군)
현재 평가 표본 45곳의 11배.  §9 이득 임계값 σ/√45 = 3.7%p 를 크게 넘는다
```

**⚠️ 오해 방지 — 안양 주차장이 늘어난 게 아니다.**
GITS 안양 89곳은 도시공사 89곳과 **같은 원천**(이름 89/89·면수 89/89 일치).
늘어난 것은 **학습 표본**이다. 외부 451곳으로 배워서 안양 89곳을 더 정확히 맞힌다.

**leave-one-city-out은 검증 방법이지 학습 방법이 아니다.**
최종 서비스 모델은 안양 포함 500곳 전부로 학습한다. 결정 ④가 그 안전장치다.

---

## 5. 데이터 현황

### 📌 기준 숫자

| 출처 | 숫자 | 라벨 | 역할 |
|---|---|---|---|
| **GITS 실시간** | **813곳** (노외 500) | ⭕ 5분 | **학습 표본 + 라벨** |
| **도시공사 포털** | **89곳** | ⭕ 5분 | 안양 라벨 (GITS와 동일 원천) |
| GITS 기본정보 | 1,010곳 | ❌ | 유형·요금·운영시간 |
| 경기데이터드림 표준데이터 | 107곳 | ❌ | 안양 공영 모집단·급지 |
| 공단 시설정보 (안양) | 7,456곳 / 293,466면 | ❌ | 주변 경쟁 피처 |
| 공단 실시간 (안양) | **0곳** | ❌ | **폐기** |

⚠️ 곳 수는 **항상 고유 ID 기준**. `prk_center_id`는 유니크하지 않다(안양 내 중복 32건).

### ① 안양도시공사 통합주차포털 — 🟢 가동 중

```
GET https://parking.auc.or.kr/api/parking/searchParkingList
```
인증·파라미터 없음. 89곳. `PARK_COUNT / CELL_CNT` = 점유율. 좌표·급지·요금 결측 0.

**✅ v8의 「신청서 기재 금지」는 해제됐다.** GITS 안양 89곳과 이름·면수가 완전히 일치하는
**같은 원천**이므로, 정식 개방 API(GITS)로 동일 데이터를 얻는다고 기재할 수 있다.
단 제공기관은 경기도이므로 §2의 안양시 데이터 문제는 별개로 남는다.

### ①-2 ★★ 89곳 중 22곳은 실시간 데이터가 없다 (v14 신규 · 치명)

`parking.db` 122,820행 전수 검사 결과:

```
전 기간 점유율 변동폭 < 1%p :  22곳 / 89     ← 전체 행의 24.7%
그중 park_count == cell_cnt 로 완전 고정 : 12곳
변동폭 1~5%p 인 곳 :  0곳                     ← 중간이 없다
```

**✅ 주말 포함 6일(162,514행)로 재확인했다. 22곳 그대로, 변동폭 여전히 `0.00`,
주말 데이터로 되살아난 곳 0곳.** 평일 편중 때문에 생긴 착시가 아니다.

**변동폭이 「작은」 게 아니라 정확히 `0.00` 이다.** 살아있거나 죽어있거나 둘 중 하나다.
저활동이 아니라 **피드 자체가 안 도는 것**이다.

| 주차장 | 면수 | 5일 내내 |
|---|---:|---|
| 시청앞노상 | 34 | 100.00% 고정 |
| 평촌역노상 | 71 | 84.51% 고정 |
| 범계역노상 | 161 | 61.49% 고정 |
| 동안노상 | 88 | 43.18% 고정 |
| 명학역노상 | 87 | 100.00% 고정 |
| 인덕원1노상 | 51 | 100.00% 고정 |

**범계역·평촌역·시청앞이 5일 내내 상수인 건 물리적으로 불가능하다.**
`park_count == cell_cnt` 12곳은 전형적인 「센서 없음 → API가 정원을 그대로 반환」 패턴이고,
나머지 10곳은 특정 시점 스냅샷에서 얼어붙었다.

**⚠️ 데모가 지금 상태로는 성립하지 않는다.**
`serve_pipeline` 카드 예시 5곳 중 **시청앞노상·동안노상·평촌역노상·범계역노상 4곳이 죽은 피드**다.
살아있는 건 평촌지하 하나뿐. 지금 시연하면 화면에 상수만 뜬다.

**대응 순서**
1. 도시공사 API 응답에 **관측시각 필드**가 있는지 확인 (GITS는 `ocrnDt` 로 freshness 판정 가능)
2. 없으면 **「값이 N시간 이상 불변」 휴리스틱**으로 stale 판정
3. **GITS 값으로 대체 가능한지 대조** — 89곳 전부 GITS에 매칭된다. 되면 22곳을 되살릴 수 있다 (최대 이득)
4. 못 살리면 서비스에서 **「실시간 미제공」 표시 + 추천 순위 제외** (`unlabeled` 로직에 얹는다)

### ①-3 ★ 운영시간 외가 표본의 과반이고, 점유율이 더 높다

살아있는 67곳 기준:

```
운영시간 내 44.1%   ·   운영시간 외 55.9%      ← 미운영이 과반
```

| | 평균 점유율 | 중앙값 | 평균 \|15분 변화\| |
|---|---:|---:|---:|
| 운영 중 | 39.3% | 34.3 | 3.31 |
| **미운영** | **66.2%** | **72.7** | 1.18 |

**미운영 시간대 점유율이 27%p 더 높다** — 밤에 주민이 노상주차장에 세워두기 때문이다.
그리고 **미운영 구간의 35.4%에서 값이 실제로 움직인다.** 죽은 구간이 아니다.

**→ 운영시간 외를 학습·예측에서 빼면 안 된다.** 이유 셋:
1. 우리 서비스 취지가 「저렴한 공영주차장」인데 **운영시간 외는 무료**다. 핵심 추천 대상이다
2. 밤 점유율이 **다음날 아침의 시작점**을 결정한다
3. 사용자가 밤 11시에 「내일 9시」를 물으면 horizon 출발점이 미운영 구간이다

평일 운영시간 분포: 09~19시 20곳 · 09~18시 12곳 · 09~17시 10곳 · 10~18시 9곳 ·
09~20시 9곳 · **00:00~23:59 7곳(진짜 24시간)** · 09~22시 7곳 · … · `00:00~00:00` 1곳(인덕원환승)

### ② 경기데이터드림 「주차장 정보 현황(제공표준)」

안양 **107행 / 7,710면**. 노상 29 · 노외 78 · 부설 0 · **공영 100%**. 유료 71 / **무료 36**.

**⚠️ 31개 컬럼 중 실사용 가능 ~12개**

| 상태 | 수 | 컬럼 |
|---|---|---|
| **전부 공란** | 9 | 운영요일, 추가단위시간/요금, 1일주차권 2종, 월정기권, 결제방법, 특기사항, **장애인전용주차구역** |
| **값 한 종류** | 6 | 주차장구분, 부제시행, 주차기본시간, 관리기관명, 전화번호, 데이터기준일자 |
| 좌표 | — | **47/107** → 60곳 지오코딩 필요 |

무료 노외 36곳은 **급지가 전부 `'기타'`** — 이들이 v8의 전이 타깃이었고, 지금은 §8의
「추정치 배지」 대상이다. (v8에 "~18~25곳"으로 적혀 있던 것은 **36곳이 맞다.**)

### ③ 한국교통안전공단 API — 시설정보만 사용

`https://apis.data.go.kr/B553881/Parking` · 일일 10,000회 × 엔드포인트 3개 각각

**실측 확정 사양 (재검증 금지)**

| 항목 | 값 |
|---|---|
| 지역·ID 필터 | **없음.** pageNo 방식이 유일 |
| `numOfRows` 상한 | **50,000** (문서의 1,000은 무의미) |
| 268B XML / 60.1초 | 서버 게이트웨이 타임아웃 → 재시도로 흡수 |
| items 위치 | `j["오퍼레이션명"]` — `body["items"]` 아님 |
| `totalCount` | **과대 보고** (1,767,934 신고 / 실수신 1,508,686 = 85%) |
| **`PrkRealtimeInfo`** | **안양 0곳. 폐기** — 경기 31개 시군 중 4곳만 연계 |

안양 7,456곳 / 293,466면 확보 (`prk_plce_adres_sigungu.startswith("안양")` 기준).
용도는 **반경 500m 경쟁 면수** 하나.

### ④ ★ 경기도 교통정보센터(GITS) — 🟢 신규 채택

```
https://openapigits.gg.go.kr/api/rest/{operation}
serviceKey(필수) · laeId(선택)     ※ apiKey/key/authKey 는 인식 안 됨
응답 XML: ServiceResult > msgBody > itemList*   (JSON 없음)
headerCd  0=정상 · 5=키 파라미터 없음 · 7=키 사용중지 · 8=요청제한 초과
```

| 오퍼레이션 | 곳 |
|---|---|
| `getParkingPlaceInfoList` 기본정보 | 1,010 |
| `getParkingPlaceAvailabilityInfoList` 실시간 | 813 |
| └ 그중 **노외** | **500** (안양 49 + 외부 451) |

화성 103 · 부천 94 · 성남 56 · 안양 49 · 평택 46 · 의정부 43 · 의왕 40 · 파주 34 ·
이천 11 · 안산 9 · 안성 8 · 군포 4 · 광주 2 · 과천 1

**실시간 확인 (10분 간격 2회)**
```
잔여면수 변동 375/810곳 (46%) · ocrnDt 갱신 806곳 (100%)
안양 89곳 중 27곳 변동
```

**★★ `avblPklotCnt` 는 「잔여」가 아니라 「주차된 대수」다 — v12까지 반대로 적혀 있었다**

필드명이 `avbl`(available)인데 **값은 주차 대수**다. 필드명이 거짓말한다.

```
도시공사:  PARK_COUNT    = 주차된 대수   →  점유율 = PARK_COUNT / CELL_CNT
GITS:      avblPklotCnt  = 주차된 대수   →  점유율 = avblPklotCnt / pklotCnt
                                              ↑ 둘이 같다. 빼지 말 것
```

**실증 (안양 85곳 전수, 도시공사와 이름 매칭)**

| 가설 | \|GITS − 도시공사\| 중앙 | 평균 |
|---|---|---|
| **`avbl` = 주차 대수** | **0.0000** | **0.0061** |
| ~~`avbl` = 잔여 (v12까지의 문서)~~ | 0.6901 | 0.6297 |

행 단위로도 정확히 일치한다: 공업부지노외 `22대` = `avbl 22` · 관악3노상 `42` = `42`.

**v9~v12 문서가 계속 `(총−잔여)/총` 으로 적고 있었고, 그걸 「부호 함정」이라고 강조까지 했다.
강조한 내용이 틀렸으므로 더 위험했다.** 813곳 전체 점유율이 뒤집힐 뻔했다.

**제공 필드 (실시간 노외 500곳 기준 채움률)****제공 필드 (실시간 노외 500곳 기준 채움률)**

| 필드 | 채움률 | 대응 |
|---|---|---|
| `pkplcTypeNm` 노상/노외/부설 | 100% | **`is_nosang` — 공단에서 판정 실패했던 필드가 여기선 그냥 온다** |
| `pkplcDivNm` 공영/민영 | 100% | 모집단 분리 |
| `pklotCnt` | 100% | `log_cells` |
| `wkday/sat/hldy OprtStart·EndTime` | 100% | `open_hours` · `free_now` |
| `latCrdn`/`lonCrdn` | 99% | `compet_*` |
| `parkingBscFare` | 63% | 요금 |
| `parkingBscTime`·`addUnitTime`·`addUnitFare`·`ddPktckFare`·`mmCmmtktFare` | 확인 필요 | **요금 계산의 핵심** |

없는 것: **급지**(표준데이터에만). `grade`는 이미 측정상 무용으로 제외됨.

**미확인:** 호출 한도. 문서에 수치 없고 `headerCd 8`만 정의돼 있다.
5분 폴링 = 288회/일. **자동 백오프로 대응하고, 1688-9090 확인은 병행.**

### ④-2 GITS 운영 실측 (9/4)

**⚠️ `pkplc_id` 는 시군 간에 중복된다**
```
pkplc_id "00001" 을 쓰는 시군: 15개 (여주·광주·안양·의왕·수원·안성·평택·남양주·
                                 부천·안산·이천·화성·파주·시흥·성남)

곳 수 세기:  pkplc_id 만          →  152곳  ❌
            (lae_id, pkplc_id)   →  823곳  ✅
```
PK를 `(ts_kst, lae_id, pkplc_id)` 로 잡아 **데이터 손실은 없다.**
문제는 집계·조인·lag 계산에서 `lae_id` 를 빼면 **15개 시군이 한 주차장으로 뭉개진다**는 것.
에러 없이 조용히 틀린다. `prk_center_id` 유니크 아님과 같은 계열.

**⚠️ 이상치 — 부호 정정 후 재해석** (818행 기준)

`avblPklotCnt` 가 주차 대수이므로 v12의 해석을 바꿔야 한다.

| 값 | 건수 | 재해석 | 처리 |
|---|---|---|---|
| `avbl` < 0 | **8** (성남 7) | **주차 대수가 음수** — 진짜 오류 | **제거** |
| `pklotCnt` ≤ 0 | **8** | 0으로 나누기 | **제거** |
| `avbl` > `pklotCnt` | **3** | **면수 초과 = 만차 초과 주차** | **정상. `clip(0,1.2)`** |

세 번째는 오류가 아니다. **도시공사에도 같은 현상이 있다**(관악노상15 `21대/19면`,
관악노상22 `32/30`). 이중 주차·경차 구획 등으로 실제 발생한다. 버리면 만차 정보를 잃는다.

음수 8건 중 7건이 성남시다. **시군별 데이터 품질 차이 — 도시 고정효과의 또 다른 근거.**

**⚠️ 안양 곳 수가 스냅샷마다 흔들린다 (구조적 4곳 차이가 아니다)**
```
기본정보(gits_lots)      89곳 전부 존재
실시간 스냅샷            81~88곳 (68개 스냅샷, 중앙 83)
```
**→ `occ_lag_5m` 계산 시 직전 스냅샷에 그 주차장이 없을 수 있다.**
§8이 lag를 최강 피처로 지목했으므로, **결측 처리를 안 하면 조용히 틀린다.**

**시군별 실시간 곳 수 (노외)**
화성 144(103) · 부천 111(94) · **안양 85(47)** · 수원 80(0) · 남양주 70(0) · 성남 64(64) ·
평택 56(46) · 의정부 46(43) · 의왕 40(40) · 고양 36(0) · 파주 34(34) · 과천 17(1) ·
이천 10(10) · 안산 9(9) · 안성 8(8) · 군포 6(4) · 광주 2(2)

⚠️ **안양이 85곳이다** (도시공사 89곳과 4곳 차이). 시점 문제인지 구조적인지 확인 필요.

### ⑤ 카카오모빌리티 길찾기 API — ✅ 실호출 검증 완료

**제휴 불필요.** 카카오디벨로퍼스 REST API 키(`Authorization: KakaoAK`)로 사용.
(제휴는 쿼터 상향 또는 장소 30개 초과 시에만). 호스트 `https://apis-navi.kakaomobility.com`

**다중 목적지 길찾기 — 사용자 검색 1회 = API 호출 1회**
```
POST /v1/destinations/directions
body {"origin":{"x":126.9226,"y":37.4018},              ★ x=경도, y=위도
      "destinations":[{"x":lon,"y":lat,"key":"3"}, …],   최대 30개
      "radius":5000}

응답 routes[]{result_code, key, summary{distance(m), duration(초)}}
```
- 10곳·25곳 요청 전건 `result_code 0` 성공
- **`key` 로 요청·응답을 매칭한다. 순서에 의존 금지**
- **쿼터 관련 응답 헤더가 없다** — 잔여 호출수를 알 수 없으므로 카운팅은 우리가 한다

**미래 운행 정보**
```
GET /v1/future/directions
    origin="경도,위도" · destination="경도,위도"
    departure_time="YYYYMMDDHHMM"   ★ 반드시 미래. 과거를 주면 실패
    summary=true
응답 routes[0].summary{distance, duration, fare{taxi, toll}}
```

### ★ 도착시간 정책 (v10 확정)

**카카오가 주는 실제값만 쓴다. 폴백 상수를 맞추는 데 시간을 쓰지 않는다.**

v9에서 폴백 상수를 실측 보정(우회계수 1.62 / 15.6km/h)했는데,
그건 **출발지 1곳·경로 25개** 기준이다. 신뢰할 만한 상수를 얻으려면
출발지 × 시간대 × 도로 위계를 교차한 대규모 검증이 필요하고, 그건 별도 연구다.
**D-13에 할 일이 아니다.**

| | 정책 |
|---|---|
| **정상** | 카카오 실제값. 근사 사용 안 함 |
| **장애 시** | 거친 근사로 서비스를 유지하되 **"실시간 경로 조회 실패 — 추정치" 명시** |
| 폴백 정확도 | **목표를 두지 않는다.** 상수 재보정 금지 |
| 후보 필터링 | 직선거리 사용 (이건 폴백이 아니라 정상 로직) |

**근거:** 소요시간은 "언제 도착하나"를 정하는 중간 변수다.
우리 차별점인 **혼잡도 예측과 요금 계산은 카카오와 무관**하므로,
카카오가 죽어도 핵심 기능은 살아 있다. 폴백은 "반쪽으로라도 돌아간다"를 보장할 뿐이다.

### ⑥ 카카오 로컬 API — 대체 주차장 표시용

```
GET https://dapi.kakao.com/v2/local/search/category.json?category_group_code=PK6
Authorization: KakaoAK {REST_API_KEY}
```
일 10만 회 무료(계정에서 **처음 활성화한 앱**에만). **카카오맵 제품 활성화 필요** — 안 하면 403.

**일반인이 실제로 댈 수 있는 주차장만 나온다.** 아파트·사무실 부설은 애초에 안 나오므로
공단 7,456곳의 유형 판정 실패를 우회한다. 실시간은 없고 위치·이름만 — 그거면 충분하다.

### ⑦ TMAP 보행자 경로안내 API — 도보 시간 (필수)

**② 최단거리 축은 「주차장 → 목적지 도보 시간」이다.** 차 거리가 아니다.
차로 가까운 주차장과 걸어서 가까운 주차장은 다르다(일방통행·좌회전 금지).

**⚠️ 직선거리 근사 사용 금지.** v11까지 폴백으로 두었던 `직선 × 1.2 ÷ 67m/분` 은
**TMAP 실패 시 최후의 보루로만** 남기고, 정상 경로에서는 절대 쓰지 않는다.

**✅ 실호출 검증 완료 (9/6). 아래는 확정 사양이다.**

```
POST https://apis.openapi.sk.com/tmap/routes/pedestrian?version=1&format=json
Header  appKey: {TMAP_APP_KEY} · Content-Type: application/json
Body    startX/startY (주차장) · endX/endY (목적지)      ★ X=경도, Y=위도
        startName/endName · reqCoordType/resCoordType "WGS84GEO" · searchOption "0"
응답    features[0].properties.totalDistance(m) · totalTime(초)
        ★ 전체 거리·시간은 첫 feature 에만 있다. 나머지는 구간 안내다
```

**★ 403 의 원인은 키가 아니라 「앱에 API 상품이 안 붙어 있던 것」이었다.**
앱 상세의 `AccessKey 정보` 가 "AccessKey가 있는 상품이 없습니다" 상태면 게이트웨이가
라우팅 전에 막아 **6개 엔드포인트 전부** `403 INVALID_API_KEY`(`category:"gw"`)가 된다.
「경로안내」 상품을 붙이자 **키 재발급 없이 통했다.**

**★ 응답에 제어문자가 섞인다.** 22건 중 1건(4.5%)이 `JSONDecodeError` 로 죽어
**조용히 폴백에 떨어졌다.** `r.json()` 은 strict 파서다 →
**`json.loads(r.text, strict=False)`** 로 재파싱. 수정 후 24/24 성공·폴백 0.

**쿼터: 경로안내 그룹 Free 1,000건/일** — 자동차·보행자·타임머신·이미지·화물차가 **공유**한다.
Lite 10,000/일 · Premium 11원/건. **캐시 없으면 후보 8곳 검색 125회로 소진된다.**

**도보 실측 (n=24, 안양 6개 목적지 × 최근접 4곳)**

| | 중앙값 | 사분위 |
|---|---:|---|
| 우회계수 (직선 50m 이상, n=23) | **1.43** | 1.24~1.63 |
| 보행속도 (n=24) | **4.2 km/h** | 3.1~4.6 |

⚠️ 직선 50m 미만은 우회비가 발산한다(인덕원2노상 직선 2m / 도보 105m = 60배). 통계에서 제외.

**★ 차 우회계수 1.62 ≠ 도보 우회계수 1.43.** 도보가 더 직선에 가깝다 —
일방통행·좌회전 금지가 없기 때문. **「도보 축을 차 거리로 대신하면 안 된다」가 수치로 확인됐다.**
폴백 상수는 1.43 / 4.2km/h 로 갱신했으나 **정상 경로에서는 쓰지 않는다.** 더 맞추지 않는다.

**캐시 — `data/interim/walk_cache.sqlite` · PK `(parking_id, 격자위도, 격자경도)`**

★ **격자 스냅이 전혀 작동하지 않는 버그를 잡았다.** `dlon` 을 **원본 위도**로 계산해
점마다 셀 크기가 달라졌다. → **스냅된 위도**로 계산하도록 수정.

| 100m 반경 200개 좌표 → 셀 수 | |
|---|---|
| 수정 전 | **200개** (스냅 무효) |
| 수정 후 | **9개** — 적중률 **22배** |

호출 카운터는 `walking.CALLS = {"tmap","cache_hit","fallback"}` 로 노출한다.

**호출 절약 — 캐시 필수**
TMAP은 단건 API라 후보 N곳이면 N회다. 반경 1km 기준 평균 7~13곳.
`(parking_id, 목적지 100m 격자)` 를 키로 캐시하면 재검색 시 호출이 0이 된다.
**주차장 좌표는 고정**이므로 캐시 적중률이 높다.

---

## 6. 수집 인프라

### 🟢 안양 89곳 (2026-08-31 04:56 KST~)

| 항목 | 값 |
|---|---|
| 호스트 | GCP e2-micro `anyang-parking-507119` / `anyang-poller` / `us-east1-c` |
| 계정 / OS | `chabee2027` / Ubuntu 24.04, TZ Asia/Seoul |
| 서비스 | systemd `anyang-poll`, `Restart=always` |
| 주기 | 5분 |
| 로그 | `~/anyang-parking/logs/poll.log` · `journalctl -u anyang-poll -f` |

**9/4 점검 결과 — 건강했다**
```
112,318행 · 1,262 스냅샷 · 4일치
간격 중앙 5.00분 · 7분 초과 갭 2회(최대 10분) · 결측률 0.16%
```

### ⚠️ 로컬 pull 정지 (9/5~9/6) — **데이터 손실 없음. 폴러는 정상이었다**

한때 「수집 중단 사고」로 판단했으나 **오진이었다.** GCP 폴러는 계속 돌고 있었고
**로컬 `auto_pull` 만 멈춰서** 로컬 DB가 9/4 23:55 에 얼어 있었다. pull 후 전량 회수됐다.

```
회수 후:  parking.db  162,514행   2026-08-31 04:56 ~ 2026-09-06 13:10
          gits.db     383,805행   2026-09-04 09:45 ~ 2026-09-06 13:05
```

**요일 분포 — 주말 확보됨**
```
월 20,292 · 화 25,632 · 수 25,632 · 목 25,632 · 금 25,632 · 토 25,543 · 일 14,151(진행 중)
```

**⚠️ 그래도 남는 문제 두 개 — 이건 실재한다**

1. **A10~A15 는 얼어 있던 구버전 DB(122,820행 · 평일 5일)로 돌렸다.**
   현재 DB는 162,514행이고 **데이터의 22%가 모델이 본 적 없는 주말**이다. 전부 재실행해야 한다
2. **이틀간 로컬이 stale 인 걸 아무도 몰랐다.** 그 사이의 모든 분석이 낡은 데이터 기반이었다

**→ `scripts/watch_freshness.py` 필요.**
두 DB의 `max(ts_kst)` 가 현재보다 20분 이상 낡으면 경고. cron 10분 주기.
**폴러 감시가 아니라 「내가 보고 있는 데이터가 최신인가」 감시다.** 이번에 걸린 게 그거다.

### 🟢 GITS 813곳 (9/4 가동)

| 항목 | 값 |
|---|---|
| 서비스 | systemd `anyang-gits`, `Restart=always` |
| 주기 | **5분** + `headerCd 8` 시 10분 자동 감속, 연속 3회 성공 시 복귀 |
| DB | `data/raw/gits.db` — `PRIMARY KEY(ts_kst, lae_id, pkplc_id)` |
| 규모 | 813곳 × 288회 = **23만 행/일**, 20일 470만 행 ≈ 250MB |
| 스크립트 | `src/collect/fetch_gits.py` — `probe / info / rt-test / poll` |

**9/4 가동 확인:** 벽시계 격자 정렬(:10 :15 :20) · `interval=300s` · 디스크 13%

### 시각 정렬 — 두 폴러 공통

```python
next_t = (time.time() // INTERVAL + 1) * INTERVAL   # 벽시계 격자
while True:
    time.sleep(max(0, next_t - time.time()))
    fetch_once()
    next_t += INTERVAL                               # "지금+300"이 아니라 "이전 목표+300"
    if next_t <= time.time():                        # 밀리면 건너뛰고 로그
        missed = int((time.time()-next_t)//INTERVAL)+1; next_t += missed*INTERVAL
```
`sleep(300)`을 쓰면 작업 시간이 누적돼 하루 15분씩 밀린다.

### 회수

```bash
bash scripts/pull_vm.sh    # .backup 스냅샷 → scp → merge → 결측 점검
```
**⚠️ 돌아가는 DB를 `.backup` 없이 복사하면 깨진다.**

### 트러블슈팅 기록

| 증상 | 원인 / 해결 |
|---|---|
| `status=209/STDOUT` 무한 재시작 | GCP 사용자명이 `chabee2027`이라 `/home/ubuntu/…` 없음. 유닛을 `<<EOF`(따옴표 없이)로 다시 써서 `$USER`/`$HOME` 치환 |
| nano 붙여넣기 들여쓰기 깨짐 | `cat > 파일 <<'PYEOF'` |
| 공단 API `Read timed out` | timeout 120 + retry 5 + 체크포인트 |
| 인증키 `SERVICE_KEY_IS_NOT_REGISTERED` | 인코딩 키 이중 인코딩. `if "%" in KEY: KEY = unquote(KEY)` |
| GITS `headerCd 7` | 키는 등록됐으나 비활성. 코드 문제 아님 — 포털 승인 대기 |
| 경기데이터드림 링크 빈 페이지 | `getDatasetDetailPage.do`는 내부 렌더용. `selectServicePage.do`가 정답 |

---

## 7. 용어 정리 — 축이 두 개다

**축 1 — 누가 운영하나:** 공영(지자체) / 민영(민간)

**축 2 — 어디에 있나**

| 용어 | 뜻 | 모습 | 차단기 |
|---|---|---|---|
| **노상**(路上) | **길 위** | 도로 가장자리 흰 선 | ❌ |
| **노외**(路外) | **길 밖** | 별도 부지·지하·주차타워 | ⭕ |
| **부설**(附設) | **붙여서 설치** | 아파트·상가·병원 딸린 것 | ⭕ |

두 축은 곱해진다. ※ **거주자우선주차**는 표준데이터에서 제외돼 있다.

### ⚠️ 포털 API의 "위탁" 함정

`PARKING_DIV_NM`에 노상/노외와 함께 **"위탁"**이 섞여 있는데 이건 **운영 방식**이다.
표준데이터 대조 결과 **위탁 14곳 중 13곳이 실제로는 노상**.

```python
df["type"]     = 노상 / 노외    # 위치  ← 표준데이터 또는 GITS pkplcTypeNm 으로 덮어쓸 것
df["operator"] = 직영 / 위탁    # 운영 방식
```

⚠️ **v10의 「GITS가 100% 주므로 해소된다」는 틀렸다.**
GITS `pkplcTypeNm` 은 **위탁 14곳을 전부 `"기타"`** 로 준다. 유형이 안 나온다.

**해법 — 주차장명 규칙 (검증됨)**
```
이름에 "노상" 포함  →  노상
이름에 "지하" 포함  →  노외
```
결과가 **노상 13 / 노외 1** 로 정본의 표준데이터 실측과 **정확히 일치**한다.
이걸로 89곳 전부 유형이 확정된다(이전엔 위탁 14곳이 계산 불가였다).

---

## 8. 기술 설계

### 전체 구조

```
[수집]  GITS 813곳 5분  +  도시공사 89곳 5분
           ↓
[학습]  경기 15개 시군 500곳 · 도시 고정효과 · horizon 피처
           ↓  선택 기준 = 안양 89곳 MAE 최소  (결정 ④)
[예측]  T분 뒤 점유율 (분위 10/50/90) + 만차확률
           ↓
[서비스] 목적지 → 후보 → 카카오 소요시간 → 도착시각 예측 → 요금 → 랭킹
```

### ★ v8과 결정적으로 달라진 것 — lag를 쓸 수 있다

v8의 모델 B는 **라벨 없는 곳**이 타깃이라 자기회귀(lag) 피처를 금지했다.
v9의 타깃은 **라벨 있는 89곳**이므로 **lag가 최강 피처가 된다.**

| | v8 모델 B | **v9** |
|---|---|---|
| 타깃 | 라벨 없는 36곳 | **라벨 있는 89곳** |
| lag 피처 | ❌ 금지 | ⭕ **핵심** |
| 실측 MAE | 24.2%p (목표 미달) | **8%p 이하 목표** |

이것이 성능 목표가 15%p → 8%p로 내려간 근거다.

### 입력 피처

**A. 자기회귀 (신규, 최강 후보)**

⚠️ **lag 결측 처리 필수.** GITS는 스냅샷마다 곳 수가 흔들린다(안양 81~88).
직전 스냅샷에 그 주차장이 없으면 `occ_lag_5m` 이 비고, 앞의 값으로 무심코 채우면 시점이 어긋난다.
**「몇 분 전 값인지」를 별도 피처(`lag_age_min`)로 넣고, 일정 시간 이상 낡으면 결측으로 둘 것.**

`occ_lag_5m` · `occ_lag_30m` · `occ_lag_1h` · `occ_lag_1d`(전일 동시각) · `occ_lag_1w`(전주 동요일 동시각)
`occ_trend_30m`(최근 30분 기울기) · `occ_ma_1h`

**B. 시간 — ★ v14에서 「운영시간 상대시각」으로 강화**

`horizon_min`(15/30/60/120) ★ · `hour_sin/cos` · `dow` · `is_weekend` · `is_holiday`

**운영시간 피처는 절대시각이 아니라 상대시각이어야 한다.**
주차장마다 운영시간이 다르다(09~19시 20곳 · 09~18시 12곳 · 10~18시 9곳 …).
`hour` 하나로는 「종료 30분 전」을 표현할 수 없다.

```
is_operating(target_time)   ★ 예측 시점이 아니라 도착 시점 기준
min_to_open                   개시까지 남은 분
min_to_close                  종료까지 남은 분
is_free_now(target_time)      무료 여부 (일요일·공휴일·운영시간 외)
```

종료 직전엔 빠지고 개시 직전엔 몰린다. **그 전이 구간이 예측이 가장 어려운 곳**이다.

**전부 조례로 고정된 값이라 미래도 알 수 있다 = known-future covariate. 누수 없다.**
`lag_24h` 보다 이쪽이 먼저다 — `lag_24h` 는 하루가 통째로 날아가지만
운영시간 피처는 첫날부터 전부 쓸 수 있다.

**★ 주말 상호작용 (v15 추가 · 우선순위 높음)**

```
is_weekend × hour_sin/cos       ← 낮 시간대 점유율이 주말에 +15~18%p (§8.5)
parking_id × is_weekend         ← 주차장마다 통근 의존도가 다르다
is_sunday                        ← 일요일 무료(비고 4). 토(57.6%)와 일(63.4%)도 다르다
```

LightGBM 은 트리라 상호작용을 어느 정도 자동으로 잡지만, **`parking_id` 가 89 레벨
categorical 이라 `× is_weekend` 를 명시적으로 주는 편이 분할 깊이를 아낀다.**
평일 5일만으로 학습된 A15 는 이 축을 전혀 갖고 있지 않다.

**C. 정적**
`log_cells` · `is_nosang`(GITS) · `is_public`(GITS) · `is_underground` · `open_hours` · `city`(고정효과) ★

**D. 공간**
`compet_n_500` · `compet_cells_500` (공단 29.3만 면 기준) · `nearest_station_dist`

**E. 환경**
`temp` · `rain` (AWS 시간별)

**제외 확정:** `grade`(타깃 상수 + 민영엔 개념 없음) · `oneday_amt` · `hourly_rate` ·
`hndcap_ratio` · `is_wital` — 전부 **타깃에 값이 없거나 한 종류뿐**
**용도지역 · 공시지가** — 측정 결과 개선 0.0%p (§17)

### ★ 요금 계산 — 조례 별표 기반 누진제 (v10 전면 수정)

**v9까지의 「기본요금 + 고정 단가」 사양은 틀렸다.** 조례 별표 1이 **시간대별 누진 할증**이다.
GITS `addUnitFare` 는 **첫 구간(30~60분) 값만** 준다. 전 구간에 곱하면:

| 주차 시간 | v9 사양 | **실제 조례** | 오차 |
|---|---|---|---|
| 1시간 | 1,500 | 1,500 | — |
| **2시간** | 3,300 | **5,100** | **−35%** |
| **3시간** | 5,100 | **10,500** | **−51%** |

요금 비교가 추천의 축 하나이므로 이대로면 **순위가 통째로 뒤집힌다.**

#### 별표 1 — 1회 주차요금 (단위: 원, 개정 2025.12.31)

| 구분 | 급지 | 최초 30분 | ~1시간<br>10분당 | ~2시간<br>10분당 | ~3시간<br>10분당 | 3시간 이후<br>10분당 |
|---|---|---|---|---|---|---|
| **노상** | 1급지 | 600 | 300 | 600 | 900 | 1,200 |
| | 2급지 | 400 | 200 | 400 | 600 | 800 |
| | 3급지 | 200 | 100 | 300 | 400 | 400 |
| **노외** | 1급지 | 600 | 300 | 600 | 900 | 1,200 |
| | 2급지 | 400 | 200 | 400 | 600 | 800 |
| | 3급지 | 200 | 100 | 300 | 400 | 400 |
| | 4급지 | 200 | 100 | 200 | 200 | 200 |
| | 5급지 | 200 | 100 | 100 | 100 | 100 |

구간 경계: `최초 30분` → `30~60분` → `60~120분` → `120~180분` → `180분~`

**교차 검증됨:** `parking.db` 의 `dflt_amt` 고유값 600/400/200 이 급지 1/2/3~5 와 정확히 대응.
**최초 30분 열은 실측 확인.** 누진 단계는 우리 데이터에 없어 **조례가 유일한 출처**다.

⚠️ 원본 대조 필요: 노외 1급지 월정기권 자리에 `90,000`·`70,000` 이 함께 추출됨 (열 누락 의심).
부설주차장 행은 급지 없이 `최초30분 700 · 10분당 300 · 일일 10,000` 구조 — 누진 단계 유무 확인 필요.

#### 별표 5 — 일일주차권 상한 (운영시간 × 급지)

| 운영시간 | 1급지 | 2급지 | 3급지 | 4급지 | 5급지 |
|---|---|---|---|---|---|
| 8시간 | 10,000 | 7,000 | 5,000 | 2,000 | 1,500 |
| 9시간 | 11,000 | 8,000 | 5,000 | 2,000 | 1,500 |
| 10시간 | 13,000 | 9,000 | 6,000 | 3,000 | 2,500 |
| 11시간 | 14,000 | 10,000 | 7,000 | 3,000 | 2,500 |
| 12시간 | 15,000 | 11,000 | 7,000 | 3,000 | 2,500 |
| 13시간 | 16,000 | 12,000 | 8,000 | 4,000 | 3,500 |

**실측 검증 2건 (일치)**
```
범계역노상  1급지 · 09:00~22:00 = 13시간  →  별표5 16,000  =  oneday_amt 16000  ✅
평촌지하    3급지 · 10:00~18:00 =  8시간  →  별표5  5,000  =  oneday_amt  5000  ✅
```
→ 도시공사 `oneday_amt` 를 상한으로 쓰는 판단이 조례로 확인됐다.
GITS `ddPktckFare` 배제도 유지(도시공사와 2/89만 일치, 값이 불규칙).

**✅ `oneday_amt` 전수 대조 완료 — 74/80 일치 (92.5%)**

불일치 6곳: 관악3노상·삼봉노상(일일권 자리에 **25,000 = 누진 일 상한액**이 들어감) ·
예술공원고가밑 · 먹거리촌노상1·2·3.
**→ 별표5 재계산값을 정본으로, `oneday_amt` 는 참고로만.**
24시간 운영 9곳은 표 밖이라 **일일권 상품 없음 + 25,000 상한만** 적용.

#### ★ 조례 비고 — 표에 없고 원문에만 있는 조항 4개

| 비고 | 내용 | 영향 |
|---|---|---|
| **4** | **일요일·법정공휴일 무료** | **주말 요금이 통째로 0.** 요금 축 랭킹이 무의미해지고, 수요 패턴도 달라진다 |
| **8** | **1일 주차요금은 선불로만 납부 가능. 후불 시 누진요금제 반영** | **일일권은 자동 적용이 아니다** (아래) |
| **10** | 누진요금제 **일 최대 25,000원** | `oneday_amt` 25,000의 정체. 월정기권이 아니라 **누진 일 상한액**이다 |
| **11** | **100원 미만 절사** | 감면 **후** 절사. 순서 주의 |

비고 9(*"1·2급지 최대 4시간까지 할증"*)는 표의 마지막 열(3시간이후)과 어긋난다.
**표를 정본으로 한다** — 4시간 초과 구간은 어차피 25,000 상한에 먼저 걸린다.

#### ★★ 일일권은 「자동 상한」이 아니라 「선불 상품」이다 (v11 정정)

v10의 `min(계산액, 일일권)` 은 **틀렸다.** 비고 8에 따르면 일일권은 **입차 시 선불로 사야 하고,
안 사면 누진 요금이 그대로 부과**된다.

```
그냥 대면       = min(누진 계산액, 25,000)          ← 후불
일일권 사면     = 별표5[운영시간][급지]              ← 선불, 표 밖이면 상품 없음
```

노외 1급지 300분 기준: **선불 16,000원 vs 후불 24,900원 — 8,900원 차이.**

**→ 두 금액을 모두 보여주고, 일일권이 유리하면 「입차 시 일일권 구매」를 안내한다.**
나가면서는 못 산다. 이걸 모르고 그냥 대면 손해를 본다.
**이걸 알려주는 앱이 없다 — 기능이 하나 늘어난 것이다.**

일일권 유리 교차점: **221분(3시간 41분)** (1급지 13시간 운영 기준, 실측 계산)

#### 별표 2 — 감면

```
전액 면제:  15분 미만 이용 · 긴급자동차
50% 감면:  경형자동차 · 저공해차(경유 제외) · 경로우대 · 심하지 않은 장애인
           ※ 전기차 충전 시 최초 2시간 면제
2시간 면제 후 초과분 50%:  국가유공자 · 심한 장애인   (병원 진료 목적은 영수증 확인 시 전액)
```

**`15분 미만 무료` 는 기본 로직**에, 나머지는 UI 옵션으로. **카카오맵·티맵 어느 쪽도 안 하는 기능이다.**

#### 계산 순서

```
1. 주차 구간을 운영시간 내/외로 분리 (요일별 운영시간 · 공휴일)
2. 운영시간 외 구간            →  0원      ← 관측의 50.4%
3. 운영시간 내 누적 15분 미만   →  0원
4. 누진 계산 (위 5단계, 10분 단위)
5. min(계산액, 25,000)          ← 비고 10 누진 일 상한
6. 감면 적용
7. 100원 미만 절사              ← 비고 11
※ 일요일·법정공휴일이면 2~7 건너뛰고 0원   ← 비고 4
※ 일일권은 별도 상품으로 병기 (자동 적용 금지)
```

**⚠️ 운영시간 외가 중간에 끼는 경우**(22시 입차 → 다음날 11시 출차)를 반드시 처리.
운영시간 내 누적 분만 누진 구간에 카운트한다.

#### ★ 서비스에 노출할 파생 정보

```
일일주차권이 더 싸지는 시점 =  1급지 13시간 운영 기준 약 3시간 45분
```
**"3시간 넘게 대실 거면 일일권 사세요"** — 누진을 제대로 구현하면 자동으로 나오는 정보이고,
이걸 알려주는 앱이 없다.

### ★ 급지의 정의 — 조례로 확인 (v8 실패의 설명)

> 가. 노상 1급지: **상업/주거/공업지역 중 주차수요가 활성화 된 지역**
> 나. 노상 2급지: **중로(12m) 이상의 도로**
> 다. 노상 3급지: 그 외
> — 2개 이상 급지에 걸치면 **상위 급지** 적용
> — 다만 **시장이 형평·효율을 위해 필요하다고 인정하면 급지를 조정할 수 있다**

**급지 = 용도지역 + 도로 위계 + 시장 재량.**

v8에서 공시지가로 급지를 대체하려다 ρ 0.532로 미달했는데, **정의의 절반만 관측 가능**했기 때문이다.
「주차수요 활성화」와 「시장 재량」은 데이터로 잡을 수 없다.
같은 행정동에 급지가 섞여 있던 것(7개 동 중 6개)도 재량 조항으로 설명된다.
**→ "급지는 주소로 못 채운다"는 v9 결론이 조례로 확정됐다.**

### 랭킹

**② 축은 도보 시간이다.** TMAP 실측값을 쓰고, 직선거리 근사를 표시값으로 쓰지 않는다.
**도보 15분(약 1km) 초과는 「멀어요」 경고**를 붙인다. 1km를 걷게 하면서 추천이라 할 수 없다.


```
① 최저요금순   ② 최단거리순      ← 2축 고정. 가중합으로 섞지 않는다
만차확률 > 컷오프 → 강등, 다음 순위가 1순위로
```
**가중합 하나로 섞으면 사용자가 추천 이유를 못 읽는다.** 네이버지도가 "최적/최소시간/최소환승"을
분리해 두는 것과 같은 이유.

### 검증 (타협 불가)

- **시간 순서 분할** — 과거로 학습, 미래로 평가. 랜덤 분할 절대 금지
- **leave-one-city-out** — 도시 단위로 빼서 전이 가능성 측정 (§9)
- **캘리브레이션** — isotonic + calibration plot. "만차확률 70%라 한 것 중 실제 70%가 만찼다"
- **분위 예측** — LightGBM `objective='quantile'` 10/50/90
- **폴백 경로 테스트** — 카카오 API가 죽어도 서비스가 살아야 한다

---

## 8.5 ★ 실험 결과 (A10~A15) — 숫자는 전부 재산출 대상

⚠️ **아래 A10~A17 은 구버전 DB(122,820행 · 평일 5일) + 시간순 80/20 분할 기준이다.**
**§8.6 의 롤링 오리진 결과가 이 절의 결론 상당수를 뒤집었다.** 인용 전에 §8.6 을 먼저 읽어라.

### 태영이 보고 (9/6 · 구버전 `parking.db` 122,820행 · 8/31~9/4 · purged chronological split)

| Horizon | Persistence | A12 시간 | **A15 시간+ID** | A12 대비 |
|---|---:|---:|---:|---:|
| 15분 | 1.784 | 1.917 | **1.784** | −6.9% |
| 30분 | 2.964 | 3.022 | **2.489** | −17.7% |
| 60분 | 5.018 | 4.858 | **3.408** | −29.8% |
| 120분 | 8.705 | 7.855 | **4.517** | −42.5% |

**채택:** 현재점유율 · lag 5/10/15/30/60 · 변화량 · rolling 15/30/60 · 시간대 · **`parking_id`(categorical)**
**제외:** 상권(개선 없음) · 환경×시간(악화)
**보류:** GITS `compet_occ_500` · 날씨

`parking_id` 중요도가 horizon 따라 2574 → 3429 → 4169 → 4932 로 증가한다.
먼 미래일수록 「이 주차장이 평소 이 시간 이후 어떻게 움직이나」가 중요해진다는 뜻이다.

**중간에 target leakage 를 발견해 purged split 으로 수정했다** — train 의 현재 시각뿐 아니라
**`target_time` 도 split 이전**이어야 한다. (태영이가 자체 점검에서 잡았다)

### ★★ 위 숫자를 그대로 쓰면 안 되는 이유 3개

**① MedianAE 는 전 horizon 에서 Persistence 가 이긴다**

| | Persistence | A15 |
|---|---|---|
| 15분  MAE / **MedianAE** / P90AE | 1.784 / **0.00** / 5.02 | 1.784 / 0.449 / **4.84** |
| 120분 MAE / **MedianAE** / P90AE | 8.705 / **1.64** / 26.67 | 4.517 / 1.93 / **12.64** |

ML 이 이기는 건 MAE 와 **P90AE** 다. 즉 **평균이 정확해진 게 아니라 큰 실수를 없앴다.**

서비스 관점에선 이게 더 낫다 — 치명적인 건 「3%p 오차」가 아니라 「여유 있다더니 만차」다.
**발표 문구는 「평균 오차 감소」가 아니라 「최악 오차를 절반으로」여야 한다.**

**② 89곳 중 22곳(행의 24.7%)이 6일 내내 상수다** → §5-①-2

**③ 표본의 큰 몫이 「예측할 게 없는 구간」이다**

살아있는 67곳 기준 평균 |15분 변화량|:
```
02시 0.2   05시 1.0   08시 3.5   09시 4.8   12시 2.5   18시 4.2   23시 0.6
```
새벽과 아침 러시가 **24배** 차이 난다.

### 조건별 재측정 — Persistence 15분 (v14 실측)

| 조건 | MedianAE | MAE | P90AE | 15분 뒤 무변화율 |
|---|---:|---:|---:|---:|
| 전체 89곳 · 전 시간 **(= 현재 보고 기준)** | **0.00** | 1.60 | 4.55 | 65.7% |
| 살아있는 67곳 · 전 시간 | 0.00 | 2.12 | 5.56 | 54.4% |
| **살아있는 67곳 · 09~20시 (= 실제 서비스 조건)** | **1.09** | **3.14** | **8.33** | **40.9%** |

**MAE 가 2배다.** 지금 보고 중인 모든 숫자는 실제 서비스 오차를 **1.5~2배 과소평가**하고 있다.
「5분 단위라 15분 뒤에도 안 변하는 게 절반 이상」도 착시였다 — **65.7% → 40.9%**.

### ★★ 주말 패턴이 평일과 완전히 다르다 (v15 신규 · 주말 데이터 회수로 확인)

살아있는 67곳 · 시간대별 평균 점유율:

| 시각 | 평일 | 주말 | 차이 |
|---:|---:|---:|---:|
| 00시 | 71.6 | 67.1 | −4.5 |
| 03시 | 72.1 | 69.4 | −2.7 |
| 06시 | 69.3 | 67.6 | −1.7 |
| **09시** | 47.4 | 59.8 | **+12.4** |
| **12시** | 29.4 | 47.2 | **+17.8** |
| **15시** | 29.8 | 44.8 | **+15.0** |
| 18시 | 45.4 | 48.6 | +3.2 |
| 21시 | 61.8 | 59.6 | −2.2 |

**낮 시간대에 주말이 +15~18%p 높다.** 밤에는 거의 같다.

**해석:** 평일 낮에는 통근으로 차가 빠져나가는데(주거지 노상주차장) 주말엔 안 나간다.
거기에 **일요일 무료**(비고 4)가 겹친다 — 일 평균 63.4% > 토 57.6% > 평일 52.6%.

**→ `is_weekend × hour` 상호작용이 필수다.** `hour` 단독으로는 이 패턴을 표현할 수 없고,
`parking_id × is_weekend` 도 필요하다(주차장마다 통근 의존도가 다르다).
**A15 는 이 패턴을 한 번도 본 적이 없다.**

### 평일/주말 분리 — Persistence 15분 (6일 데이터)

| 조건 | MedianAE | MAE | P90AE | 무변화율 |
|---|---:|---:|---:|---:|
| 살아있는 67곳 · 09~20시 | 1.09 | 3.14 | 8.33 | 40.9% |
| └ **평일** 09~20시 | **1.44** | **3.46** | **9.09** | 37.4% |
| └ **주말** 09~20시 | 0.00 | 1.97 | 5.37 | 54.1% |

**주말이 평일보다 예측하기 쉽다** (변동이 적다). 대신 **수준이 다르다.**
즉 주말은 「어렵다」가 아니라 「다른 문제」다. 평일 지표와 섞으면 양쪽 다 흐려진다.

**→ 이 조건에서 A10~A15 를 전부 재실행해야 한다.** 숫자는 나빠지지만 그게 진짜 숫자다.
동시에 두 가지가 함께 검증된다:
- **A15 의 −42% 가 죽은 22곳을 뺀 뒤에도 유지되는가**
- **주말이 들어온 뒤에도 유지되는가** (지금 모델은 주말을 학습한 적이 없다)

### A13·A14 판정 정정 — 「악화」가 아니라 **「판정 불가」**

| 실험 | 기존 기재 | **정정** |
|---|---|---|
| **A13** GITS `compet_occ_500` | 악화 | **판정 불가.** train 10,324행 = A15 의 **10.6%**, overlap 14시간. 그 구간 Persistence MAE 1.36 < 전체 1.78 = **야간 편중 표본**. 이 조건에선 어떤 ML 도 진다 |
| **A14** 날씨 | 악화 | **판정 불가.** 악화폭 +0.22~1.23% = **noise**. 8/31~9/4 에 유의미한 강수가 있었는지 확인 전엔 결론 못 낸다. `is_rain` 중요도 0 은 「비가 안 왔다」일 수 있다 |

**발표에서 "날씨는 효과 없습니다"라고 말하면 반박당한다.**
**"관측 기간에 강수 이벤트가 없어 판정을 보류했습니다"가 정확하다.**

⚠️ A14 정합성 자체는 검증됐다 — Open-Meteo 144시간, 결측 0, backward `merge_asof`,
122,820행 **coverage 100%**. 파이프라인은 살려두고 강수 있는 기간에 재실행한다.

---

## 8.6 ★★ 롤링 오리진 검증 — 40/40 (v16 · 이 절이 §8.5 를 대체한다)

`src/report/rolling_origin_simple.py` · `reports/tables/u9_rolling_origin_simple.csv`

**expanding window · test 는 다음 하루 · purged (label time 도 같은 구간이어야 함)**

```
fold 1  train 8/31~9/2   → test 9/3(목)      평일
fold 2  train 8/31~9/3   → test 9/4(금)      평일
fold 3  train 8/31~9/4   → test 9/5(토)      주말
fold 4  train 8/31~9/5   → test 9/6(일)      주말
fold 5  train 8/31~9/6   → test 9/7(월, 부분) 평일
```

### 결과 — ML 개선율 % (음수 = ML 승)

| 구간 | h | fold1(평) | fold2(평) | fold3(토) | fold4(일) | fold5(평) |
|---|---:|---:|---:|---:|---:|---:|
| **운영 중** | 15 | −1.5 | −0.6 | −1.7 | −6.1 | −1.8 |
| | 30 | −7.4 | −8.6 | −4.8 | −7.7 | −6.2 |
| | 60 | −17.5 | −15.5 | −0.3 | −19.7 | −12.7 |
| | 120 | **−29.2** | −23.9 | −0.8 | −27.7 | −18.2 |
| **운영 외** | 15 | −7.3 | −2.5 | −3.0 | −3.8 | −11.2 |
| | 30 | −16.9 | −17.6 | −5.9 | −7.1 | −22.7 |
| | 60 | −36.1 | −29.7 | −9.1 | −11.8 | −33.2 |
| | 120 | **−55.0** | −46.2 | −4.1 | −23.1 | −40.1 |

**전 fold × 전 horizon × 전 구간 = 40/40 칸에서 ML 이 persistence 를 이겼다. 패배 칸 0.**
전체 세그먼트(평일/주말/전체 포함)까지 넣어도 패배 칸은 **없다.**

### ★★ a17 의 「주말 붕괴」는 모델 문제가 아니라 분할 문제였다

a17 에서 주말이 **+107~223%** 무너진 것은 시간순 80/20 분할의 아티팩트다:

```
a17 (시간순 80/20)      train 주말   0%  ·  test 주말 100%    ← 극단적 분포 이동
롤링 오리진 fold 4       train 에 9/5(토) 포함                ← 주말을 하루라도 학습
```

**주말을 하루만 학습해도 주말 전 horizon 에서 ML 이 이긴다.**
따라서 v15 의 *"주말을 배운 적 없는 모델은 주말에 못 쓴다"* 는 유지되지만,
*"9/12~13 이 진짜 측정 시점"* 이라는 단서는 **완화된다** — 이미 측정됐다.

### 발표 문구 (독창성·AI혁신성)

> 단일 시간순 분할에서는 주말 예측이 무너지는 것처럼 보였다. 원인은 모델이 아니라
> **검증 설계**였다 — 마지막 20%가 통째로 주말이라 학습 데이터에 주말이 0%였다.
> 5-fold rolling origin 으로 바꾸자 **40/40 칸에서 ML 이 기준선을 이겼다.**

§17 의 *"검증 지표 자체를 검증해야 한다"* 와 같은 계열의 교훈이고, 서사가 일관된다.

---

## 8.7 라우터 폐기 (v16 결정)

**U3 의 「horizon × 구간별로 persistence/ML 중 검증 성능이 좋은 쪽 선택」을 폐기한다.**

**근거:** §8.6 에서 persistence 가 이기는 칸이 **하나도 없다.** 고를 것이 없으면 라우터는
분기 비용과 버그 표면만 남긴다. 실제로 현재 라우터는 다음 상태다:

```
15  outside|we  persistence      ← 롤링 오리진에서 ML 이 3.0~11.2% 이기는 구간
30  outside|we  persistence      ← ML 이 5.9~22.7% 이기는 구간
60  outside|we  persistence      ← ML 이 9.1~36.1% 이기는 구간
120 outside|we  ml
```

**4칸만 살아남았고(운영중·평일 키가 없어 기본값 `"ml"` 로 폴백), 살아남은 3칸이 전부 틀렸다.**
**라우터가 켜진 채로는 성능이 깎인다.**

- `predictor.predict()` 에서 라우팅 분기를 제거하고 항상 ML 을 쓴다
- `router.json` · `u3_router.md` 는 **폐기 근거와 함께** §17 로 이관한다
- 9/12~13 이후 재측정에서 persistence 가 이기는 칸이 생기면 그때 되살린다

**⚠️ persistence 는 기준선으로는 계속 유지한다.** 폐기하는 것은 「라우팅」이지 「비교」가 아니다.

---

## 9. ablation — leave-one-city-out

v8에서 "지역 축 폐기 → 피처 축 전환"이었는데 **GITS로 지역 축이 부활했다.**

### ★★ 분할 방식 — 롤링 오리진이 표준이다 (v16 확정)

**시간순 단일 80/20 분할을 금지한다.** 6일 데이터에서 마지막 20%는 통째로 주말이라
train 주말 0% · test 주말 100% 가 되고, 그것이 a17 의 「주말 붕괴」를 만들었다(§8.6).

```
expanding window · test 는 다음 하루 · purged (label time 도 같은 구간)
fold k:  train = [시작 … d_k−1]   test = d_k
```
- **평일 fold 와 주말 fold 를 각각 확보**한다. 지금은 평일 3 · 주말 2
- fold 별 n 을 반드시 병기한다 (fold 1 은 train 3일뿐이라 얇다)
- 새 데이터가 쌓이면 fold 가 자동으로 늘어난다. **스크립트로 유지할 것**

### ★ 평가 층화 원칙 (v14 신설) — 하나의 MAE 로 뭉치지 않는다

**주 지표 = 살아있는 67곳.** 그 안에서 두 축으로 나눈다:

| 살아있는 67곳 | 평일 | 주말 |
|---|---|---|
| **운영시간 내** | ★ | ★ |
| **운영시간 외** | ★ | ★ |

죽은 22곳은 네 칸 전부 **참고**로만 낸다 (상수를 맞히는 건 성능이 아니다).

**「빼는 것」이 아니라 「나누는 것」이다.** 셋을 혼동하지 말 것:

| | 어떻게 |
|---|---|
| 운영시간을 **입력 피처로** 넣나 | **넣는다** ✅ (§8-B) |
| 운영시간 외를 **학습에서** 빼나 | **안 뺀다** ❌ |
| 운영시간 외를 **평가에서** 빼나 | **안 뺀다. 나눠서 본다** |

- **예측·학습은 네 칸 전부 한다.** 운영시간 외는 행의 55.9%이고 점유율이 오히려 27%p 높다(§5-①-3)
- **평가만 나눠 보고한다.** 뭉치면 어느 칸에서도 실제 성능이 아닌 숫자가 나온다
- 죽은 22곳은 상수를 맞히는 것이라 성능이 아니다 → **주 지표에서 분리**

- **평일/주말을 섞지 않는다.** 낮 점유율이 15~18%p 다르고 변동성도 다르다(§8.5).
  섞으면 양쪽 다 흐려진다

*"평일 낮 MAE 3.5, 주말 낮 2.0, 밤 1.2"* 가 *"전체 MAE 1.78"* 보다 심사에서 강하다.
후자는 **"왜 이렇게 좋냐"고 파고들면 무너진다.**

### 판정 부등식 (유지)

```
넓히는 게 이득 ⟺  지역 편향 δ  <  σ / √n_T
n_T = 45 (안양 노외) →  임계값 3.7%p
```
표본이 45뿐이라 임계값이 매우 관대하다. **외부가 웬만큼 편향돼 있어도 섞는 게 이득이다.**

### 실험 표

| 학습 데이터 | 안양 MAE | 안양 Spearman [95% CI] |
|---|---|---|
| 안양 89곳만 | ? | ? |
| + 인접 시군 (군포·의왕·안산·과천) | ? | ? |
| **경기 15개 시군 500곳** | ? | ? |
| 경기 500곳 + **도시 고정효과** | ? | ? |
| **leave-one-city-out** (안양 빼고 학습 → 안양 예측) | ? | ? |

**마지막 행이 그 자체로 전이 사례다** — "한 도시로 학습해서 처음 보는 도시에 적용".
997건 중 이걸 한 팀이 없다.

### 피처 그룹 ablation (병행)

| 피처셋 | MAE |
|---|---|
| 정적만 | ? |
| + 시간 | ? |
| **+ lag** ★ | ? |
| + 주변 경쟁 | ? |
| + 날씨 | ? |

**모든 상관계수에 n과 95% 신뢰구간을 병기할 것.** (v8에서 n=19의 −0.279를
"부호가 뒤집혔다"로 오독한 전례가 있다. Fisher z 기준 CI가 [−0.65, +0.20]으로 0을 포함했다.)

---

## 10. 성능 목표

**★ v14 — 모든 목표치는 「살아있는 67곳 · 운영시간 내」 기준이다.**
전체 89곳·전 시간 기준 숫자는 실제 오차를 1.5~2배 과소평가한다(§8.5).

| 용도 | 합격선 | 근거 |
|---|---|---|
| **30분 예측** | **MAE ≤ 8%p** | 서비스 조건 Persistence 15분이 3.14 이므로 여유가 있다 |
| 60분 예측 | MAE ≤ 12%p | |
| 120분 예측 | MAE ≤ 15%p | |
| **만차 판정** | **AUC ≥ 0.85, 정밀도 ≥ 0.8** | 랭킹 강등의 근거 |
| 캘리브레이션 | 기울기 0.9~1.1 | "신뢰도가 높다"의 유일한 증거 |

**보고는 항상 네 칸으로 나눈다** (§9 평가 층화). 하나로 뭉친 MAE 는 발표에서 방어가 안 된다.

### 주력 모델과 딥러닝 정책 (v14 개정)

**주력은 LightGBM 이다.** 근거는 관측 행 수가 아니라 **독립 표본 = lot 수(89, GITS 813)** 이고,
LSTM 계열은 파라미터가 5~10만인데 독립 계열이 89개다. M5 대회에서도 우승과 상위 대부분이 GBDT 였다.

**단 「딥러닝 금지」는 v14에서 「주력 금지, 비교군으로 권장」으로 바꾼다.**
심사 문구가 *"AI 학습·분석 도구를 **효과적으로 활용**했나"* 이므로,
**"왜 이 모델인가"에 답하는 비교표가 있는 편이 점수가 높다.**

| 모델 | 역할 |
|---|---|
| **LightGBM (lag + 시간 + parking_id)** | ★ 주력 |
| Persistence | 기준선 (필수) |
| LSTM 또는 GRU 1종 | **ablation 비교군 1행** — 시간이 남으면 |

**분위 예측 필수:** `objective='quantile'` α = 0.1 / 0.5 / 0.9 + **CQR 보정**.
`full_prob` 는 분위 보간이 아니라 **직접 binary 분류기 + log-odds 에 fit 한 Platt** 로 낸다
(분위 3점 보간 + isotonic 은 확률이 두 값으로 붕괴한다 — 실측 확인).

### ★ 구간 커버리지 (v16 신설 — 합격 기준)

```
커버리지 = mean(p10 ≤ 실제값 ≤ p90)        목표 0.80 ± 0.03  (0.77 ~ 0.83)
```

**양쪽 다 실패다:**

| 상태 | 증상 | 실제 사례 |
|---|---|---|
| **과신** (커버리지 ↓) | 구간이 좁아 자주 빗나감 | U8 이전: 120분 예측인데 폭 **4.1%p** |
| **과대** (커버리지 ↑) | 항상 맞지만 정보가 없음 | CQR 직후: 커버리지 0.93 · 폭 **36%p** |

*"40~76% 사이입니다"* 는 맞아도 쓸모가 없다. **0.93 은 통과가 아니라 실패다.**

- 커버리지가 0.77~0.83 을 벗어나면 **`[p10, p90]` 을 UI·fixture 로 내보내지 않는다**
- 현재 상태: 운영 중 0.78~0.87 (대체로 통과) · **운영 외 0.84~0.88 (과대)**
- 보고는 fold × horizon × 구간으로, **커버리지와 평균 폭을 항상 병기**한다

**신뢰도 판정 기준은 Brier 단독이 아니라 「reliability 기울기 + 커버리지」다.**
persistence 의 Brier 는 하드 0/1 예측이라 쉬운 문제에서 구조적으로 유리하다 —
만차 판정이 95% 정확하면 Brier ≈ 0.05 인데, 확률 모델은 0.7 을 내면 맞아도 0.09 를 잃는다.

---

## 11. 리포지토리 구조

```
anyang-parking/
├── CLAUDE.md · README.md · requirements.txt · .env(gitignore)
├── data/
│   ├── raw/        parking.db · gits.db · std_parking.csv
│   │               kotsa_v2_*.jsonl · gits_info.csv
│   ├── interim/    lots.parquet · obs.parquet · geocode_cache.csv
│   └── processed/  features.parquet
├── src/
│   ├── collect/    poll_parking.py★ · fetch_gits.py★ · fetch_kotsa.py · geocode.py
│   ├── features/   spatial.py · temporal.py · lags.py · build.py
│   ├── serve/      recommend.py · fare.py · routing.py      ← 신규
│   ├── analysis/   a01~a05 · loco.py
│   ├── models/     train.py · evaluate.py · calibrate.py · ablation.py
│   └── utils/      merge_db.py · geo.py · kr_holidays.py
├── web/            지도 + 검색 + 추천 UI                     ← 신규
├── reports/        figures/ · tables/
└── scripts/        pull_vm.sh · check_gaps.py
```

`reports/`를 고정 경로로 두는 이유: **분석 산출물이 곧 사업계획서 그림**이다. 파일명에 날짜 금지.

---

## 12. 작업 순서

| 날짜 | 할 일 | 담당 | 상태 |
|---|---|---|---|
| 8/31 | 안양 89곳 폴링 가동 | A | ✅ |
| 9/1 | 사전 검증 게이트 · 전이 설계 판정 | A | ✅ |
| 9/4 | GITS 확보 · 아이템 전환 | 둘 다 | ✅ |
| 9/4 | GITS 폴링 GCP 이관 · 조례 별표 확보 · 카카오 3종 검증 | 둘 다 | ✅ |
| 9/5 | 요금 계산기 누진제 재작성 · `oneday_amt` 전수 대조 | A | ✅ |
| 9/6 | **TMAP 연동 · 도보 실측 · 캐시 · 후보선정 · 추천 조립** | A | ✅ |
| 9/6 | A10~A15 실험 (persistence → lag → 시간 → 공간 → 날씨 → ID) | A | ✅ |
| 9/5~6 | **첫 주말 데이터 확보** (토 25,543 · 일 14,151) | — | ✅ |
| 9/6 | 로컬 pull 복구 — 162,514행 회수. **데이터 손실 없음** | A | ✅ |
| **9/6 즉시** | **🔴 죽은 22곳 원인 규명 → GITS 대체 가능성 대조** | A | ⬜ |
| **9/6** | `scripts/watch_freshness.py` — 로컬 DB stale 감시 (20분) | A | ⬜ |
| **9/7** | **평가 층화(살아있음 × 운영시간 × 평일/주말) 도입** | A | ⬜ |
| **9/7** | **★ A10~A15 를 새 DB(주말 포함)로 전부 재실행** | A | ⬜ |
| **9/7** | **`is_weekend × hour` · `parking_id × is_weekend` 상호작용 투입** | A | ⬜ |
| **9/8** | **분위 모델(α .1/.5/.9) + isotonic → `full_prob`** | A | ⬜ |
| **9/8** | **`predictor` 를 `recommend.py` 에 주입 → 엔드투엔드 데모 성립** | A | ⬜ |
| 9/9 | 운영시간 상대시각 피처(`min_to_open/close`) 투입 | A | ⬜ |
| 9/9 | 하이브리드(horizon별 persistence/ML 선택) 확정 | A | ⬜ |
| **9/10** | **★ 실측 2~3곳 반나절** (9/5 미실시분) · 인덕원환승 24시간 확인 | 둘 다 | ⬜ |
| 9/8~15 | 웹 UI (지도 + 검색 + 추천 카드) | B | ⬜ |
| 9/10 | 만차확률 컷오프 확정 · TMAP 데모 시나리오 **캐시 워밍** | 둘 다 | ⬜ |
| 9/7 | **롤링 오리진 5-fold — 40/40 ML 승** | A | ✅ |
| **9/7 즉시** | **🔴 U9 산출물 커밋 · 라우터 폐기** | A | ⬜ |
| **9/7~8** | **🔴 태영 UI — fixture 인계 → 스크린샷 3장** | B | ⬜ |
| 9/8 | 커버리지 운영 외 과대 조정 (0.84~0.88 → 0.77~0.83) | A | ⬜ |
| 9/9 | U4 순위 민감도 롤링 오리진으로 재실행 | A | ⬜ |
| **9/12~13** | **두 번째 주말 — 재현성 확인 (fold 자동 증가)** | — | ⬜ |
| 9/14 | A13(GITS)·A14(날씨) 롤링 오리진 조건으로 재실험 | A | ⬜ |
| 9/15 | 롤링 오리진(expanding window 3-fold) 재검증 + LOCO | A | ⬜ |
| 9/16 | `lag_24h` / `lag_7d` (데이터 7일 이상 확보 시) | A | ⬜ |
| **9/17** | **기능 동결. 결과 표·스크린샷** | 둘 다 | ⬜ |
| 9/17~20 | 사업계획서 10p | B | ⬜ |
| 9/19~20 | 서명·스캔·PDF 병합(10MB) | B | ⬜ |
| **9/21** | 제출 | — | ⬜ |

### 🔴 지금 막힌 것 — 우선순위 순

1. **라우터가 성능을 깎고 있다** — 4칸만 남았고 살아남은 3칸이 전부 틀렸다(§8.7). 폐기할 것
2. **U9 산출물 미커밋** — 40/40 결과가 untracked CSV 에만 있다
3. **스크린샷 0장** — `web/` UI 미착수. **완성도 20점의 최대 리스크**
4. **커버리지 운영 외 과대** — 0.84~0.88. 이 상태로는 `[p10, p90]` 을 UI 에 못 낸다
5. **죽은 센서 22곳** — 순위 제외 + 완전 카드로 확정됐으나, 시내 중심 커버리지 공백은 남는다
6. **안양시 제공 데이터 0건** — 25점 vs 22점. 여전히 미해결

### 역할 (2인)

| | 담당 |
|---|---|
| **A 데이터·모델** | 폴링 운영 / 피처 파이프라인 / 모델·검증·ablation / 요금 계산기 |
| **B 프론트·문서** | 웹 UI / 카카오 API / **이가령 주무관 문의** / 안양시 데이터 발굴 / 사업계획서 / 서류 |
| 공통 | **9/5 실측**, 매일 `check_gaps.py` |

---

## 13. 하지 말 것

**요금**
- ❌ **GITS `addUnitFare` 를 전 구간에 곱하기** — 첫 구간(30~60분) 값이다. 누진제를 무시하면 2시간에 35% 틀린다
- ❌ **GITS `ddPktckFare` 사용** — 도시공사 `oneday_amt` 와 2/89만 일치, 정의 불명
- ❌ 별표5 표 밖(8시간 미만·13시간 초과) 운영시간을 보간·추정 — 상한 없음 + 로그
- ❌ 조례 요금표를 API에서 가져오려 시도 — **어느 API에도 누진 단계가 없다.** 상수로 박아야 한다
- ❌ **일일권을 `min()` 으로 자동 적용** — 선불 상품이다(비고 8). 두 금액을 병기할 것
- ❌ 일요일·공휴일에 요금을 부과 (비고 4: 무료)
- ❌ 감면 전에 100원 절사 (비고 11: 감면 후)
- ❌ **GITS `pkplcTypeNm` 으로 위탁 유형 판정** — 위탁 14곳을 전부 "기타"로 준다. 주차장명 규칙 사용
- ❌ 부설주차장 요금을 추천에 사용

**도보**
- ❌ **도보 시간을 직선거리 근사로 표시** — TMAP 실측값만 쓴다. 근사는 TMAP 장애 시 최후 수단이고 그때도 「추정치」 명시
- ❌ 도보 축에 자동차 거리 사용
- ❌ 반경을 3km 넘게 확장

**도착시간**
- ❌ **폴백 상수를 재보정하는 데 시간 쓰기** — 출발지 1곳·25경로로 맞춘 값은 신뢰할 수 없고, 제대로 하려면 별도 연구다. **카카오 실제값만 쓴다**
- ❌ 폴백 발동 시 추정치임을 숨기기
- ❌ 카카오 응답을 순서로 매칭 — `key` 로 매칭할 것

**부호·단위**
- ❌ **GITS `avblPklotCnt` 를 잔여로 착각** — 필드명은 `avbl` 이지만 **값은 주차 대수**다.
  점유율은 `avblPklotCnt / pklotCnt`. **빼면 안 된다.** (85곳 전수 실증: 중앙 오차 0.0000 vs 뒤집으면 0.6901)
- ❌ 카카오 좌표를 `위도,경도` 순으로 — **`경도,위도`** 다
- ❌ **GITS 곳 수를 `pkplc_id` 만으로 세기** — 시군 간 중복이다. **`(lae_id, pkplc_id)`** 조합 필수
- ❌ 점유율 이상치를 거르지 않고 학습에 투입 — `avbl<0`(8건) · `pklotCnt≤0`(8건)은 **제거**
- ❌ **`avbl > pklotCnt` 를 오류로 보고 버리기** — 만차 초과 주차이고 도시공사에도 있다. `clip(0,1.2)` 로 살릴 것
- ❌ **lag 계산 시 결측을 앞 값으로 무심코 채우기** — 스냅샷마다 곳 수가 흔들린다. `lag_age_min` 을 함께 넣을 것
- ❌ 면적을 WGS84 경위도로 계산 (투영좌표 변환 필수)

**피처**
- ❌ **타깃에 없는 피처를 최종 피처셋에 포함** — 학습은 되는데 예측할 값이 없다
- ❌ **표준데이터 컬럼을 채움률 확인 없이 투입** — 9개는 전부 공란, 6개는 값 한 종류
- ❌ `grade`·용도지역·공시지가 재시도 (측정 완료, 무용)

**검증**
- ❌ 랜덤 train/test 분할 (인접 시각 누수)
- ❌ **신뢰구간 없이 상관계수 보고**
- ❌ 학습 집단을 평가 집단과 같이 좁히기 — 타깃 조건을 재현할 대상은 **평가**지 학습이 아니다
- ❌ 기준선을 바꿔가며 개선폭 비교 (v8에서 −1.0%p 착시 발생)
- ❌ 관측치 많다고 딥러닝 — **독립 표본은 lot 수**

**검증 (v16 추가)**
- ❌ **시간순 단일 80/20 분할** — 마지막 20%가 통째로 주말이라 train 주말 0%가 된다. **롤링 오리진을 쓴다**(§9)
- ❌ **모델이 본 구간으로 라우터·하이퍼파라미터 선택** — validation 은 학습에서 빼야 한다 (U8 에서 실제로 발생)
- ❌ **커버리지 0.84~0.93 을 「목표 0.80 초과 달성」으로 보고** — **과대는 실패다**(§10)
- ❌ Brier 단독으로 확률 품질 판단 — persistence 는 하드 예측이라 구조적으로 유리하다
- ❌ **리포트 수치를 손으로 옮기기** — CSV 에서 읽어 포맷하라. 결론 문장도 표에서 파생시켜라
- ❌ **합격 기준 미달인데 `complete` 로 커밋** — `partial` 을 쓰고 미달 항목을 명시하라

**평가 (v14 추가)**
- ❌ **전체 89곳·전 시간 하나로 뭉친 MAE 를 성능으로 보고** — 실제 서비스 오차를 1.5~2배 과소평가한다. **네 칸으로 나눌 것**(§9)
- ❌ **운영시간 외를 학습·예측에서 제외** — 행의 55.9%이고 점유율이 27%p 더 높다. **평가만 나눈다**
- ❌ **MedianAE 0 을 「예측이 정확했다」로 해석** — 죽은 피드·미운영 구간이 만든 착시다. 조건 걸면 1.09
- ❌ **표본 크기·시간대 편중을 확인하지 않고 「악화」로 판정** — A13(14시간·야간 편중)·A14(강수 없음) 둘 다 「판정 불가」가 정확하다
- ❌ **단일 날짜 test 로 개선폭 확정** — A15 의 test 는 마지막 하루(9/4 금) 뿐이다. 롤링 오리진 필요

**데이터**
- ❌ **폴러 정지를 며칠 뒤에 알아차리기** — 마지막 `obs` 가 20분 이상 낡으면 알리는 감시가 필요하다. 이번엔 이틀을 몰랐다
- ❌ **"변동 0"을 계측 불량으로 단정** — 항상 만차인 주차장일 수 있다. 그게 더 중요한 정보다
- ❌ **반대로, "변동 0"을 그냥 정상으로 두기** — 89곳 중 22곳은 **5일 내내 변동폭 정확히 0.00** 이다. 저활동이 아니라 피드 정지다. `park_count == cell_cnt` 12곳은 센서 없음 패턴
- ❌ **죽은 피드 주차장을 추천 순위에 노출** — 상수를 실시간인 것처럼 보여주는 것이다
- ❌ 5분보다 짧은 폴링 / `.backup` 없이 DB 복사
- ❌ 포털 `div`의 "위탁"을 위치 유형으로 사용 (GITS `pkplcTypeNm`으로 덮어쓸 것)
- ❌ **`prk_center_id`를 PRIMARY KEY로 사용** (유니크 아님)
- ❌ 공단 `numOfRows` 상한 / 지역 필터 재탐색 (각각 50,000 · 없음으로 확정)
- ❌ 곳 수를 행 수로 세기

**서비스**
- ❌ **아파트·사무실 부설을 대체 주차장에 표시** — 일반인이 못 들어간다
- ❌ 카카오 API 실패 시 서비스가 같이 죽는 구조 (근사 폴백 필수)
- ❌ 최저요금·최단거리를 가중합 하나로 섞기
- ❌ `reports/` 파일명에 날짜

---

## 14. 사업계획서 유의

### 독창성 — 선행 수상작을 직접 인용하고 차별점 명시

| 선행작 | 성격 | 우리와의 차이 |
|---|---|---|
| 안양 2024 최우수 「주정차 위반 단속」 | 행정·단속 | 우리는 **시민 대면 추천** |
| 수원 2025 우수 「불법 주정차 단속 효율화」 | 행정·단속 | 동일 |
| 울산 2024 우수 「노상주차 패턴 분석」 | 분석 | 실측 라벨 없이 수행 → 검증 불가 |
| 국토진흥원 2025 우수 「공항 주차 수요 예측」 | 분석 | 서비스가 아니라 분석 |

**주차 관련 수상작 13건이 전부 「단속」 또는 「수요 분석·입지」다. 시민 대면 추천 서비스는 0건.**

### 기존 서비스와의 차별점

| | 카카오맵·티맵 | 안양 통합주차포털 | **우리** |
|---|---|---|---|
| 현재 빈자리 | ⭕ | ⭕ | ⭕ |
| **도착 시점 예측** | ❌ | ❌ | ⭕ |
| **주차 시간별 요금 계산** | ❌ | ❌ | ⭕ |
| **무료 시간대 반영** | ❌ | ❌ | ⭕ |
| 혼잡 시 대안 재정렬 | ❌ | ❌ | ⭕ |

### 과정 자체가 근거다

- 전국 176만 곳 전수 조사로 **공단 실시간 연계가 경기 31개 시군 중 4곳뿐**임을 실측
- 그 막다른 길에서 **지자체 자체 개방 경로(GITS)를 찾아 500곳 라벨 확보**
- **낙관 편향을 스스로 찾아내 타깃 조건으로 재측정**한 이력 (§17)

*"공공데이터가 있으니 되겠지"가 아니라 **가용성을 검증하고 설계를 확정**했다.*

### 대분류 중복 리스크

안양시 2024 최우수가 「주정차 위반 단속」. 1번 항목에서
**"단속 효율화(행정)"가 아니라 "시민의 주차 탐색 비용 절감"**임을 분명히.

---

## 15. 참고 링크 — ✅ 검증 완료

### 실제로 쓰는 API

| 용도 | 주소 |
|---|---|
| **GITS 실시간 (외부 라벨)** | `https://openapigits.gg.go.kr/api/rest/getParkingPlaceAvailabilityInfoList`<br>기본정보: `…/getParkingPlaceInfoList` · 포털 https://openapigits.gg.go.kr/api/jsp/openApi_info.jsp |
| **안양 89곳 실시간** | `https://parking.auc.or.kr/api/parking/searchParkingList`<br>화면: https://parking.auc.or.kr/main |
| **카카오 길찾기** | `https://apis-navi.kakaomobility.com/v1/destinations/directions` (POST)<br>가이드 https://developers.kakaomobility.com/guide/navi-api/start.html |
| **카카오 로컬 (PK6)** | `https://dapi.kakao.com/v2/local/search/category.json` |
| 공단 시설정보 | https://www.data.go.kr/data/15099883/openapi.do |

### 경기데이터드림 데이터셋

베이스: `https://data.gg.go.kr/portal/data/service/selectServicePage.do?infId={infId}&infSeq=1`
⚠️ `getDatasetDetailPage.do`는 내부 렌더용이라 로그인 없이는 빈 페이지다.

| 데이터 | infId | OPEN API |
|---|---|---|
| **주차장 정보 현황(제공표준)** ★ | `JHB0Z0DW2XWE342WQLR312739290` | — |
| AWS 시간별 관측현황 | `458YRRY04VI3BBMI6Q8326869752` | ⭕ **매일 갱신 → API 필수** |
| 행정동 단위 상가 업소 현황 | `E5RTWGNKKYKBO2GI67KF24613201` | ⭕ |
| 경기도 공동주택 현황 | `VUPYJVKMYEYIKOQDILSR30099546` | ⭕ |
| 주정차위반단속위치 현황 | `WCKQVVNWL0D0HPPI738V21581030` | ⭕ |
| 시장 및 마트 현황 | `319KATDSS7GKZLNXWNLZ21113196` | ⭕ |

### 기타

| | |
|---|---|
| 경기데이터드림 안양시 632건 (**안양시 제공 데이터 발굴**) | https://data.gg.go.kr/portal/data/village/selectListDataByCityPage.do?sigunFlag=41170&sigunNm=안양시 |
| 자치법규정보시스템 (요금 조례 별표 1) | https://www.elis.go.kr |
| 수상작 목록 | https://www.data.go.kr/suc/awardWinning.do |
| 수상작 분석 | `claude/기존수상작-충돌검증.md` |
| 문의 | 안양시 AI정책과 **이가령 주무관 031-8045-5483** · 안양도시공사 031-389-5326 · GITS 1688-9090(미검증) |

---

## 16. 변경 이력

**v16 (9/7) — ★★ 롤링 오리진 40/40 · 「주말 붕괴」는 분할 아티팩트였다 · 라우터 폐기**
- **★★ §8.6 신설 — 5-fold rolling origin(평일 3 · 주말 2)에서 전 fold × 전 horizon × 전 구간
  = 40/40 칸 ML 승. 패배 칸 0.** 120분 기준 평일 −18~−55% · 주말 −0.8~−27.7%
- **★★ a17 의 「주말 +107~223% 붕괴」는 모델 문제가 아니라 검증 설계 결함이었다.**
  시간순 80/20 은 train 주말 0% · test 주말 100% 를 만든다. 롤링 오리진에서 train 에
  주말이 하루만 들어가도 주말 전 horizon 에서 ML 이 이긴다.
  → **§9 에 「시간순 단일 분할 금지 · 롤링 오리진 표준」 명문화**
- **★ §8.7 라우터 폐기.** persistence 가 이기는 칸이 하나도 없으므로 고를 것이 없다.
  게다가 현재 라우터는 **4칸만 남은 채(운영중·평일 키 소실) 살아남은 3칸이 전부 틀려**
  운영외·주말 15/30/60분을 persistence 로 보내며 **성능을 깎고 있었다.**
  persistence 는 **기준선으로는 유지**한다 — 폐기하는 것은 라우팅이지 비교가 아니다
- **★ §10 에 구간 커버리지 합격 기준 신설 — `0.80 ± 0.03`.**
  **과신(폭 4.1%p)과 과대(커버리지 0.93 · 폭 36%p) 둘 다 실패**임을 명시.
  기준을 벗어나면 `[p10, p90]` 을 UI·fixture 로 내보내지 않는다
- `full_prob` 구현 확정 — 분위 3점 보간 + isotonic 은 **확률이 두 값으로 붕괴**한다(실측).
  **직접 binary 분류기 + log-odds 에 fit 한 Platt** 로 교체. reliability 기울기 0.997~1.041
- 강등 컷오프 `0.5` 활성화 — 석수역 데모에서 만차 1위(도보 5분)가 실제로 5위로 밀리는 것 확인
- 죽은 피드 22곳을 **「실시간 미제공」 완전 카드**(요금·도보·면수·운영시간 포함, 순위 제외)로 확정
- §13 에 검증 관련 금지 6건 추가 (분할·누수·커버리지·Brier·리포트 작성·커밋 판정)

**v15 (9/6 오후) — 수집 사고 오진 정정 · ★ 주말 패턴 확보**
- **⚠️ v14 의 「수집 중단 사고」는 오진이었다.** GCP 폴러는 계속 돌고 있었고 **로컬 `auto_pull` 만 멈춰** 로컬 DB 가 9/4 23:55 에 얼어 있었다. pull 후 **162,514행 전량 회수 · 데이터 손실 0**. 주말도 확보(토 25,543 · 일 14,151)
  - **교훈은 그대로 남는다:** 이틀간 stale DB 인 걸 아무도 몰랐고 **그 사이 A10~A15 를 전부 낡은 데이터로 돌렸다.** `watch_freshness.py` 는 폴러 감시가 아니라 **「내가 보는 데이터가 최신인가」 감시**로 여전히 필요하다
- **✅ 죽은 22곳은 주말 포함 6일로 재확인.** 변동폭 여전히 `0.00`, 되살아난 곳 **0곳**. 평일 편중 착시가 아니었다
- **★★ 주말 패턴이 평일과 완전히 다르다 (신규).** 살아있는 67곳 기준 낮 시간대 점유율이 **주말이 +15~18%p 높다**(12시 +17.8 · 15시 +15.0 · 09시 +12.4). 밤에는 거의 같다. 평일 낮엔 통근으로 차가 빠지는데 주말엔 안 빠지고, **일요일 무료**(비고 4)가 겹친다 — 일 63.4% > 토 57.6% > 평일 52.6%
  - **→ `is_weekend × hour` · `parking_id × is_weekend` 상호작용 필수.** `hour` 단독으로는 표현 불가
  - **→ A15 는 이 패턴을 한 번도 본 적이 없다.** 재실행 필요의 두 번째 이유
- **주말은 평일보다 예측이 쉽다** (Persistence 09~20시 MAE 주말 1.97 vs 평일 3.46, 무변화율 54.1% vs 37.4%). 「어렵다」가 아니라 **「다른 문제」**다 → §9 평가 층화에 **평일/주말 축 추가**
- §8.5 상단에 **「구버전 DB 기반이므로 재실행 전까지 인용 금지」** 경고 추가

**v14 (9/6) — ★★ 수집 중단 사고 · 죽은 센서 22곳 · 평가 층화 원칙**
- **🔴 수집이 9/5 00:00 KST 에 멈췄다.** `parking.db` 와 `gits.db` 가 **동시에** 끊겼고 이틀을 몰랐다. **보유 데이터가 평일 5일(8/31 월~9/4 금)뿐이고 주말이 0일**이다. 일요일 무료(비고 4) 때문에 주말 패턴이 완전히 다른데 모델이 한 번도 못 봤다. 남은 주말은 9/12~13 하나
- **★★ §5-①-2 신설 — 89곳 중 22곳(행의 24.7%)이 5일 내내 변동폭 정확히 `0.00`.** 변동폭 1~5%p 인 곳이 **0곳**이라 저활동이 아니라 피드 정지다. 12곳은 `park_count == cell_cnt` (센서 없음 패턴). **범계역·평촌역·시청앞이 5일 내내 상수인 건 물리적으로 불가능하다.** `serve_pipeline` 데모 카드 5곳 중 **4곳이 여기 해당**한다
- **★ §9 평가 층화 원칙 신설.** 「운영시간 내/외 × 살아있음/죽음」 네 칸으로 나눠 보고한다. **빼는 게 아니라 나누는 것** — 학습·예측은 전부 하고 평가만 나눈다
- **★ §8.5 신설 — A10~A15 결과와 재해석.** 세 가지를 정정했다:
  - **MedianAE 는 전 horizon 에서 Persistence 가 이긴다.** ML 이 이기는 건 MAE 와 **P90AE**(120분 26.67 → 12.64). 즉 **평균이 아니라 최악을 줄인 것**이고, 발표 문구를 그렇게 바꿔야 한다
  - **조건을 걸면 숫자가 2배가 된다.** Persistence 15분 MAE 1.60(전체) → **3.14**(살아있는 67곳·09~20시), MedianAE 0.00 → **1.09**, 무변화율 65.7% → **40.9%**. 「5분 단위라 안 변한다」는 착시였다
  - **A13·A14 는 「악화」가 아니라 「판정 불가」.** A13 은 train 이 A15 의 10.6%(14시간·야간 편중), A14 는 악화폭 +0.2~1.2% = noise 이고 관측 기간 강수 여부 미확인
- **★ §8-B 시간 피처를 「운영시간 상대시각」으로 강화** — `min_to_open`/`min_to_close`/`is_free_now`. 주차장마다 운영시간이 달라 `hour` 로는 「종료 30분 전」을 표현 못 한다. 조례 고정값이라 known-future covariate. **현재 데이터 5일에선 `lag_24h` 보다 이게 먼저다**
- **§5-①-3 신설 — 운영시간 외가 행의 55.9%이고 점유율이 27%p 더 높다**(66.2% vs 39.3%). 미운영 구간의 35.4%에서 실제로 값이 움직인다. **운영시간 외는 무료라 우리 서비스의 핵심 추천 대상이다. 절대 빼면 안 된다**
- **§5-⑦ TMAP 확정** — 403 의 원인은 키가 아니라 **앱에 API 상품 미등록**이었다. 「경로안내」를 붙이자 재발급 없이 통했다. **응답에 제어문자가 섞여** 22건 중 1건이 조용히 폴백에 떨어졌다 → `json.loads(text, strict=False)`. **격자 캐시가 무효였던 버그**(`dlon` 을 원본 위도로 계산) 수정 → 셀 200개 → 9개, 적중률 22배. **도보 우회계수 1.43 ≠ 차 1.62** — 도보 축을 차 거리로 대신하면 안 된다는 설계 판단이 수치로 확인됐다
- **§10 딥러닝 정책 개정** — 「금지」 → **「주력 금지, 비교군으로 권장」**. 심사 문구가 *"효과적으로 활용했나"* 이므로 "왜 이 모델인가"에 답하는 비교표가 점수에 유리하다
- **§10 분위 예측을 필수로 승격** — 지금 모델은 MAE 최적화 = 조건부 평균이라 확률이 안 나온다. `full_prob` 없이는 §3 의 강등 로직이 성립하지 않는다
- **§17 에 「발전 방향 — 장기 예측」 신설** (10/30 발표용)
- §13 에 평가 관련 금지 5건 · 데이터 관련 3건 추가

**v13 (9/4 밤) — ★ GITS 부호 정정 (치명적) · lag 결측 처리**
- **★★ `avblPklotCnt` 는 「잔여」가 아니라 「주차된 대수」다.** v9~v12가 계속 `(총−잔여)/총` 으로 적고 **그것을 「부호 함정」이라고 강조**까지 했다. 틀린 내용을 강조했으므로 더 위험했다
  - 실증: 안양 85곳 전수, 도시공사 대조. **주차대수 가설 중앙 오차 0.0000 / 잔여 가설 0.6901**
  - 필드명 `avbl`(available)이 값과 반대다. **필드명을 믿지 말라는 이 프로젝트의 원칙이 또 확인됐고, 이번엔 문서 작성자가 걸렸다**
- **이상치 재해석** — `avbl > pklotCnt` 3건은 오류가 아니라 **면수 초과 주차**다(도시공사에도 관악노상15 21/19 등 존재). `clip(0,1.2)` 로 살릴 것. 음수·총면수0만 제거
- **★ 안양 곳 수는 구조적 4곳 차이가 아니라 스냅샷마다 81~88 흔들림**(중앙 83, 기본정보엔 89곳 전부). → **lag 결측 처리 필수**. `lag_age_min` 피처 추가 권고

**v12 (9/4 밤) — 도보 경로 TMAP 확정 · 추천 흐름 정정**
- **★ ② 최단거리 축의 정의 명확화 — 「주차장 → 목적지 **도보** 시간」.** v11까지 "거리"로만 적혀 차/도보가 모호했다. 차로 가까운 곳과 걸어서 가까운 곳은 다르다
- **§5-⑦ TMAP 보행자 경로안내 API 신설. 직선거리 근사는 정상 경로에서 사용 금지**(최후 보루로만). 캐시 키 `(parking_id, 목적지 100m 격자)`
- **★ 시간이 두 종류임을 명시** — 혼잡도 예측 시점은 **차 도착**이지 목적지 도착이 아니다. 섞으면 도보 시간만큼 늦게 본다
- **★ 반경 확장 규칙 — 1km → (5곳 미만) 2km → 3km 상한.** 89곳 실측상 석수역은 1km에 1곳뿐이라 고정 반경이면 외곽에서 추천이 비어버린다
- 미래 출발 시 카카오 미래운행 API가 **단건만 지원**하므로 목적지 ETA 하나를 공통 적용. 지금 출발은 다중목적지로 주차장별 ETA
- 도보 15분 초과 시 「멀어요」 경고
- §13에 도보 관련 금지 3건 추가

**v11 (9/4 밤) — 조례 비고 반영 · 일일권 재정의**
- **★ 일일권은 「자동 상한」이 아니라 「선불 상품」** (비고 8). v10의 `min(계산액, 일일권)` 은 틀렸다. 노외 1급지 300분 기준 **선불 16,000 vs 후불 24,900** — 8,900원 차이로 순위가 뒤집힌다. **두 금액을 병기하고 「입차 시 구매」를 안내**하는 게 맞고, 이게 기능이 하나 는 것이다
- **비고 4 — 일요일·법정공휴일 무료.** 주말 요금이 통째로 0. 요금 축 랭킹과 수요 패턴 양쪽에 영향
- **비고 10 — 누진 일 상한 25,000원.** `oneday_amt` 25,000의 정체가 밝혀졌다(월정기권이 아님)
- **비고 11 — 100원 미만 절사.** 감면 **후** 적용
- `oneday_amt` 전수 대조 **74/80 일치(92.5%)** → **별표5 재계산값을 정본으로**
- **★ §7 정정 — v10의 「GITS가 유형을 100% 주므로 위탁 함정 해소」는 틀렸다.** GITS는 위탁 14곳을 전부 "기타"로 준다. **주차장명 규칙**(노상 포함→노상 / 지하→노외)으로 해소했고 결과가 **노상 13 / 노외 1** 로 표준데이터 실측과 정확히 일치
- 단위 테스트 28건 통과. 일일권 교차점 **221분** 확인
- §13에 요금 관련 금지 4건 추가

**v10 (9/4 저녁) — 요금 체계 정정 · 인프라 점검**
- **★ §8 요금 계산 전면 수정.** 조례 별표 1이 **시간대별 누진 할증**이었다. v9의 「기본요금 + 고정 단가」는 2시간에 35%, 3시간에 51% 과소계산. GITS `addUnitFare` 는 첫 구간 값만 준다
- 별표 1(1회 요금 5단계) · 별표 5(일일권 = 운영시간 × 급지) · 별표 2(감면)를 **상수로 확보**
- **별표5 실측 검증 2건 일치** — 범계역노상 13시간 1급지 = 16,000 / 평촌지하 8시간 3급지 = 5,000. `oneday_amt` 를 상한으로 쓰는 판단 확인
- ⚠️ `oneday_amt` 25,000원 다수 발견 (별표5 최댓값 16,000) — **89곳 전수 대조 필요**
- **★ 급지 정의 확인** — 용도지역 + 도로 위계 + **시장 재량**. v8의 공시지가 대체 실패(ρ 0.532)가 이걸로 설명된다
- **★ 「15분 미만 무료」·감면 기준 확보** — 카카오맵·티맵 어느 쪽도 안 하는 기능
- **결정 ⑨ 변경 — 도착시간은 카카오 실제값만.** 폴백 상수 튜닝은 하지 않는다(출발지 1곳·25경로로 맞춘 값은 근거가 얇고 제대로 하려면 별도 연구)
- §5-⑤ 카카오 3종 실호출 검증 완료 — 다중목적지(30곳/1회) · 미래운행 · 로컬 PK6(질의당 45건 상한)
- **§5-④-2 GITS 운영 실측 신설** — **`pkplc_id` 시군 간 중복**(00001을 15개 시군이 공유, 곳 수가 152 vs 823), 점유율 이상치(잔여<0 8건·총면수≤0 8건·잔여>총 3건, 성남 집중), 시군별 곳 수
- §6 폴링 점검 — 안양 89곳 112,318행 / 1,262 스냅샷 / 간격 중앙 5.00분 / 결측률 0.16%
- ⚠️ **GITS 안양이 85곳** (도시공사 89곳과 4곳 차이) — 원인 미확인
- §13에 요금·도착시간·GITS ID 관련 금지 8건 추가

**v9 (9/4) — ★ 아이템 전환. 전이 추정 → 추천 서비스**
- **§0·§3 아이템 전면 교체.** "목적지+주차시간 → 도착 시점 예측 + 요금 계산 + 공영주차장 추천". 배점 76 → 84~88
- **§5-④ GITS 신규 채택** — 실시간 노외 500곳(안양 49 + 외부 451, 14개 시군). 표본 45 → 500, **11배**
- **§4 결정 ③ 무효 → 재개.** v8의 "넓힐 데이터가 없다"는 **공단 API만 보고 내린 판단**이었다
- **§5-① 「도시공사 신청서 기재 금지」 해제** — GITS 안양 89곳과 이름·면수 완전 일치, 같은 원천
- **§2에 새 리스크** — 안양시 제공 데이터가 한 건도 없다. 25점 가점 조건에 직결
- **§8 lag 피처 해금** — 타깃이 라벨 있는 89곳이라 자기회귀를 쓸 수 있다. 목표 MAE 15%p → **8%p**
- **§5-⑤ 카카오 길찾기 확인** — 제휴 불필요, REST 키로 사용. **다중 목적지 1회 호출로 30곳**, 미래 출발시각 지원
- §9 ablation에 **leave-one-city-out** 부활
- §13에 부호 함정 2건 신설 — GITS는 **잔여**를 준다 / 카카오는 **경도,위도**
- §13 "변동 0을 계측 불량으로 단정" 금지 — 항상 만차일 수 있다
- 날짜 정정: 실측 주말은 **9/5(토)~9/6(일)**. v8의 "9/6~7"은 9/7이 월요일이라 틀렸다
- 타깃 수 정정: v8의 "~18~25곳" → **36곳** (표준데이터 무료 노외)
- v8의 전이 설계·게이트 결과를 **§18로 이관**

**v8 (9/1)** — 급지·유형 결측 우회로(도로중심선·건물 폴리곤 / 공시지가·용도지역). 이후 측정에서 전부 무용 판정

**v7 (9/1)** — 게이트 판정, 미결정 4건 확정, 공단 실시간 폐기

**v6 (8/31)** — 경기데이터드림 링크 전면 수정, 세 출처 비교표, 채움률 실측

**v5~v2** — 공단 API 조사, level×shape 설계, 용어 정리, v1 데이터 현황 전면 수정

---

## 17. 폐기된 설계 (기록용 — 발표 자료로 인용 가능)

### 전이 추정 (v2~v8의 본 설계)

**"계측되는 89곳으로 학습해서 계측 안 되는 무료 노외 36곳을 추정한다."**

| 측정 결과 | 값 |
|---|---|
| LOO MAE (89곳 전체) | 20.4%p |
| LOO MAE (노외 45곳) | 21.5%p |
| **LOO MAE (타깃 규모 19곳)** | **25.3%p** — 목표 15%p 미달 |
| oracle (천장) | 4.3%p |
| shape 오차만 / level 오차만 | 17.4 / 13.2%p |

**왜 실패했나 — 학습 집단과 타깃 집단의 분포가 분리돼 있었다**

| | 학습 89곳 | 타깃 36곳 |
|---|---|---|
| 노상 비율 | 45% | **0%** |
| 무료 비율 | 0% | **97%** |
| 급지 | 1~5 | **전부 '기타'** |
| 면수 중앙값 | 54 | 24 |

최강 피처 `is_nosang`(ρ 0.623)이 타깃에서 상수 0이 되어 정보가 통째로 사라졌다.

**split-half 진단 (발표 자료로 쓸 만함)**

```
같은 주차장, 다른 날 프로파일 상관   0.978
같은 주차장, 5분 관측 홀/짝          0.999
다른 주차장끼리                      0.339
```
→ 주차장마다 뚜렷이 다른 시간 패턴이 **실재**하는데(노이즈 아님), 현재 피처가 전혀 못 잡았다.

**살아남은 것:** 이 분석은 **§8의 「추정치 배지」** 로 서비스 안에 남는다.
라벨 없는 무료 노외 36곳을 추천 순위엔 안 넣되, 추정값을 별도 표시.
그리고 10/30 발표에서 **"다음 단계"** 로 제시한다.

### 게이트 지표 `피처거리↔프로파일유사도 ρ ≤ −0.3`

shape이 균질할 때와 피처가 무력할 때 **같은 값(0)** 이 나와 판별력이 없었다.
LOO MAE로 교체했다. **"검증 지표 자체를 검증해야 한다"**는 교훈.

### 지역 확장 판정 (v7~v8)

공단 API 기준으로 "외부 라벨 없음"이라 판정했으나, **지자체 자체 개방 경로를 확인하지 않았다.**
GITS로 뒤집혔다. **"없다"는 판정에는 "어디까지 찾아봤나"가 따라붙어야 한다.**

### 급지·유형 결측 대체 (v8)

| 시도 | 결과 |
|---|---|
| 용도지역 면적비 | LOO MAE 개선 **0.0%p** |
| 개별공시지가 | 급지와 ρ 0.532 (기준 0.6 미달) |
| 도로중심선+건물로 유형 판정 | 정확도 **28%** (다수클래스 72.9% 미달) |

유형 판정 실패의 원인은 **표준데이터 좌표가 주소 대표점**이었기 때문(107곳 중 81곳이 건물 내부).
**GITS가 `pkplcTypeNm`을 100% 제공하므로 이 문제 자체가 사라졌다.**

### 공단 실시간 폴링

안양 연계 0곳. 경기 31개 시군 중 4곳(남양주·의정부·평택·이천)뿐.
`PrkSttusInfo`와 `PrkRealtimeInfo`의 ID 체계가 분리돼 있음이 산술로 증명됨
(안 겹치는 787,082곳 > 미도달 259,248곳).
---

## 18. 발전 방향 (v14 신설 — 10/30 발표 「발전 가능성」 20점용)

### 임의 horizon 예측 — 추후 확장으로 명시

사용자가 「오늘 저녁」·「이번 주말」·「이달 말」을 묻는 건 자연스러운 요구지만,
**9/21 기준으로는 데이터가 절대 부족하다.** 보유 5일 · 주말 0일 · 남은 주말 1회.

**이건 모델 문제가 아니라 정보 문제다.** horizon 이 하루를 넘으면 예측 가능한 성분은 셋뿐이다:

```
주기성 (lot × 요일 × 시간)  +  캘린더 (공휴일·행사·무료 여부)  +  날씨 예보
```

lag 이 죽고 그래프 전파도 죽는다 — 「옆 주차장이 지금 만차」라는 정보가 2주 뒤엔 존재하지 않는다.
**따라서 어떤 모델을 써도 상한이 같다.** 결국 `E[점유율 | 주차장, 요일, 시각, 캘린더]` 추정이다.

### 1년치가 쌓이면 딥러닝으로 되나 — 모델별 평가

| 모델 | 장기 적합도 | 판단 |
|---|---|---|
| **Bi-LSTM** | ✗ 예측 불가 | 양방향은 미래 구간이 있어야 돌아간다. 예보엔 구조적으로 못 쓴다. **다만 과거 결측 보간엔 최적** — 우리 0.16% gap 과 GITS 스냅샷 누락에 쓸 수 있다 |
| **iTransformer** | △ | variate 를 토큰으로 보니 89~813곳 상호작용엔 맞다. 그러나 벤치마크 horizon 이 최대 720 step(5분이면 60시간)이라 「이달 말」은 시간단위 리샘플 필수. LTSF-Linear 가 이 벤치마크에서 선형모델이 트랜스포머를 이긴다고 반증한 영역이다 |
| **TFT** | ◎ **정답** | 우리 문제 모양 그대로다. static(급지·세대수·면수) + known-future(요일·공휴일·무료시간) + observed(점유율 이력) → **직접 multi-horizon**. **quantile 이 네이티브**라 「이번 주말 15시 만차 확률 78%」가 바로 나온다. variable selection network 로 「무엇이 중요했나」 설명도 된다 |
| **ST-GCN / AST-GCN** | ○ 단기 한정 | 도메인 매칭은 최고다. 주차장=노드, 인접=엣지, 만차 시 인접 이동=message passing. **우리 `compet_occ_500` 이 1-hop mean aggregation, 즉 손으로 만든 degenerate GCN 이다.** AST-GCN 의 attribute 조건화는 A08(세대수) 그 자체. 다만 METR-LA/PEMS 성과는 전부 15/30/60분이고 **하루 넘는 horizon 은 논문들이 평가조차 안 한다** |

### 1년치가 바꾸는 것 / 안 바꾸는 것

**바뀜:** (요일, 시각) 슬롯당 표본이 52개가 된다(현재 2개 이하). 프로파일 추정 SE 가 √26배 줄어
**그제서야 장기 예측의 상한을 측정할 수 있게 된다.**

**안 바뀜 ①:** 독립 표본 수는 여전히 **주차장 개수**(89, GITS 813)다. 시간축을 늘려도 딥러닝
파라미터를 지탱하는 건 노드 수다. **GCN 계열엔 1년치보다 813곳으로 넓히는 게 훨씬 이득이다.**

**안 바뀜 ②:** 1년이면 여름·겨울·명절·방학이 **각각 1번씩**만 관측된다. 연 단위 계절 효과는
n=1 이라 검증 불가. 「이달 말」에서 계절 요인을 분리하려면 **최소 2년**이다.

### ★ 지금 데이터로도 할 수 있는 것 — 발표에서 가장 강한 카드

장기 예측 성능의 상한은 지금 계산 가능하다:

```
잔차     = 점유율 − mean(점유율 | lot, dow, hour)
상한 R²  = 1 − Var(잔차) / Var(점유율)
```

**이게 어떤 모델도 못 넘는 천장이다.** 여기에 **horizon 별 MAE 곡선**(5분 → 1시간 → 6시간 → 1일)을
겹쳐 그려서 **언제 persistence 를 못 이기고 climatology 로 수렴하는지** 보이면,
*"왜 우리는 단기에 집중했는가"* 가 데이터로 증명된다.
심사에서 "장기는 왜 안 했냐"가 나올 때 **이 그래프 하나면 끝난다.**

### 발표 슬라이드 구성

```
단기 (≤1시간)    LightGBM + lag           ← 지금 구현
중기 (1시간~1일)  ST-GCN / AST-GCN         ← 노드 813곳으로 확장 시
장기 (1일~)      TFT 분위수 출력           ← 1년치 축적 시
```

**장기 구간은 딥러닝 이점이 작아 계층적 프로파일 모델이 이길 수도 있다고 정직하게 쓴다.**
과장하지 않는 편이 「발전 가능성」에서 오히려 점수가 나온다.
