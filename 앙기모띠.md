# 태영 — 병렬 작업 (준혁이 모델 T2.5 돌리는 동안)

파일 소유권: **너 = `web/`, `docs/`, 문서 · 준혁 = `src/`**
`src/` 는 건드리지 마. 필요한 값은 `recommend.py` 가 뱉는 JSON 으로만 받는다.

---

## 1순위 — 웹 UI (오늘 시작. 9/15 목표)

`web/` 이 비어 있다. **완성도 20점이 통째로 여기 걸려 있고, 지금 스크린샷이 0장이다.**

**입력 계약** — `recommend.py` 카드 JSON. 이 모양은 안 바뀐다:
```json
{"name":"평촌지하","parking_id":16,
 "drive_min":12,"walk_min":5,"total_min":17,
 "fare_payg":2300,"fare_daily_pass":5000,"daily_pass_better":false,
 "avail_now":550,"avail_pred":null,"full_prob":null,
 "walk_far_warning":false,"estimated":false,
 "cell_cnt":793,"straight_m":242,"arrive_at":"14:12"}
```
`avail_pred`/`full_prob` 는 지금 `null` 이다. **null 이면 "실시간만 표시"로 그리고,
값이 들어오면 예측 배지가 뜨게 만들어라.** 모델 완성을 기다리지 마라.

**화면 3개면 충분하다**
1. **검색** — 목적지 입력 + 주차 시간 + [출발 시각]
2. **결과** — 지도 + 카드 리스트. **① 최저요금순 / ② 도보 최단순 탭 2개**
   (가중합으로 섞지 마라. 네이버지도가 "최적/최소시간/최소환승" 나누는 것과 같은 이유)
3. **상세** — 요금 계산 내역 (누진 구간별) + 일일권 병기

**★ 카드에 반드시 들어가야 하는 것 — 여기가 우리 차별점이다**
```
차 12분 + 도보 5분 = 17분          ← 두 시간을 분리 표시. 합만 보여주면 안 됨
2시간 2,300원  |  일일권 5,000원    ← 병기. min() 자동적용 금지 (선불 상품임)
14:12 도착 예상 · 예상 잔여 8면      ← "지금"이 아니라 "도착할 때"
[실시간 미제공]                     ← 죽은 22곳 배지
```
- **"지금 빈자리"와 "도착 시점 예측"을 시각적으로 구분해라.** 카카오맵은 전자만 한다
- 도보 15분 초과는 「멀어요」 경고
- 일일권이 유리하면 **"입차 시 일일권을 사세요"** 안내 (나가면서는 못 산다. 이걸 아는 앱이 없다)

**단일 HTML 파일로 만들어라.** 지도는 카카오맵 JS SDK.
스크린샷 3장이 나오는 순간 사업계획서의 절반이 채워진다.

---

## 2순위 — 안양시 제공 데이터 발굴 (오늘 저녁 30분)

**3점짜리다. 25점 vs 22점.**

지금 우리가 쓰는 데이터 제공기관이 전부 안양시가 아니다:
GITS = 경기도 · 표준데이터 = 경기도 · 공단 = 국토부 · 카카오 = 민간.
**신청서에 쓸 「안양시」 데이터가 한 건도 없다.**

https://data.gg.go.kr/portal/data/village/selectListDataByCityPage.do?sigunFlag=41170&sigunNm=안양시

632건 중 **제공기관이 "안양시"인 것을 1건만** 찾으면 된다. **주제는 무관하다.**
찾으면 우리 서비스에 억지로라도 끼울 수 있는지 보고, 안 되면 그것대로 보고해라.

---

## 3순위 — 이가령 주무관 통화 (내일 9/7 월 오전)

**안양시 AI정책과 031-8045-5483** · 질문 4개:

1. 표준데이터 제공기관이 "경기도"인데 신청서 안양시 칸에 적어도 되나요?
2. 안양도시공사 주차 API 를 안양시 데이터로 적어도 되나요?
3. 안양시가 직접 개방한 데이터 목록은 어디서 보나요?
4. **★ 위탁 운영 주차장 14곳의 실시간 연계 계획이 있나요?** ← 신규

4번은 우리가 방금 찾은 거다. **89곳 중 22곳이 실시간을 안 주는데, 그중 14곳이
위탁 운영 주차장 전부다.** 직영은 67/75 가 정상이다. 우연이 아니라 구조다.
이건 시에 주는 발견이라 **답변 내용 자체가 사업계획서 재료**가 된다.

---

## 4순위 — 실측 (목적이 바뀌었다. 9/10까지)

원래는 "포털 숫자가 맞나" 확인이었는데, **죽은 피드 22곳의 물리적 증거 수집**으로 바꾼다.

| 주차장 | 면수 | 포털이 5일 내내 주장하는 값 | 급지 | 운영 |
|---|---:|---|---:|---|
| **시청앞노상** | 34 | **100% = 34대 만차** | 2 | 10~18시 |
| **평촌역노상** | 71 | 84.5% = 60대 | 1 | 10~20시 |
| 평촌지하 (대조군) | 793 | 정상 변동 | 3 | 10~18시 |

**시청앞노상 34면부터 가라. 세기 쉽고, 6일 내내 만차라고 주장 중이다.**
실제로 만차가 아니면 그 사진 한 장이 **"위탁 운영 = 실시간 미연계"의 직접 증거**다.

동시에 할 것:
- **정산 안내판 사진** — 조례 별표1 누진 구간이 실제와 맞는지 (비고 4·8 확인)
- 인덕원환승 평일 `00:00~00:00` 이 진짜 24시간 운영인지 (89곳 중 1곳)
- 평일에 가라. 일요일은 무료라 안내판 확인이 애매하다

---

## 5순위 — 서류 (지금 걸어두면 나중에 안 급함)

- **재학증명서 — 8/21 이후 발급분만 유효.** 둘 다. 오늘 발급 가능
- 서식1·3·4·5 → **팀원 전원 자필 서명 후 스캔** (9/19~20)
- 최종 PDF 1개 · **10MB 이하** · 파일명 `작품명_팀명.pdf` → karyeong@korea.kr

---

## 사업계획서 (UI 끝나면 바로. 9/17~20)

지금 재료는 충분히 쌓였다. 10p 목차만 잡아둬라:

| p | 내용 | 재료 위치 |
|---|---|---|
| 1 | 문제 정의 — "지금"이 아니라 "도착할 때" | §3 |
| 2 | 공공데이터 활용 표 (제공기관·데이터명·URL) | §15 ★ **미기재 시 가점 0** |
| 3 | 시스템 구조도 | §8 |
| 4 | AI 모델 · 검증 | §8.5 (T2.5 결과 나오면) |
| 5 | **요금 누진제 + 일일권 선불** — 아무도 안 하는 기능 | §8 별표1·5, 비고 8 |
| 6 | 도보 경로 (TMAP 실측) — 차 우회 1.62 vs 도보 1.43 | §5-⑦ |
| 7 | **UI 스크린샷** ← 네가 만들 것 | web/ |
| 8 | **행정 발견 — 위탁 14곳 실시간 미연계** | T1 |
| 9 | 기존 서비스·수상작 차별점 | §14 |
| 10 | 발전 방향 (단기 GBDT → 중기 ST-GCN → 장기 TFT) | §18 |

**8페이지가 「발전 가능성 20점」의 핵심이다.** 분석만 한 팀은 이걸 못 찾는다.